# macOS 端开发清单

> macOS 端开发遵守仓库根目录 `AGENTS.md` 的通用开始、结束、Git 协作和硬件安全规则。
> 本文件只记录 macOS 任务、验收条件和进度，不重复通用约束。

## 当前基线

- 原生 Swift/AppKit 菜单栏 App，正式工程为 `macOS/DisplaySwitcher.xcodeproj`。
- Bundle Identifier：`local.maizi.DisplaySwitcher`；最低支持 macOS 12。
- 正式构建脚本：`macOS/scripts/build-app.sh`。
- 构建产物：`macOS/outputs/DisplaySwitcher.app` 和当前架构 ZIP。
- 当前版本：2.2.0（build 20）。
- 本机配置为 `schemaVersion = 5`；v4 保留非 USB 设置并迁移到新格式，独立 USB 功能默认关闭且不猜测旧绑定或输入源。
- 双端网络运行时只接受协议 v2；v1、缺失版本、类型错误和未知版本均在入口安全拒绝。
- macOS 正式运行时只使用 Apple Silicon CoreDisplay/IOAVService 原生 DDC；Intel Mac 明确不支持，不执行外部 DDC 工具或软件调光回退。
- App Sandbox 和 Hardened Runtime 当前保持关闭，正式公证前必须评估私有 API、USB、DDC 与登录启动兼容性。

## 已完成

### M-001 Xcode 原生工程迁移

- [x] 标准 macOS Application Target 和共享 Scheme。
- [x] Xcode Debug/Release 构建和 `macOS/scripts/build-app.sh`。
- [x] App 资源、Info.plist、Framework 和本地临时签名。
- [x] 删除已经过时的 `Package.swift`。

### M-002 动态多显示器配置

- [x] 任意数量的显示器配置集合。
- [x] UUID 稳定匹配和枚举顺序变化测试。
- [x] 旧两显示器配置迁移。
- [x] 新显示器和替换显示器不继承或猜测输入源。

### M-003 开源仓库基础

- [x] MIT License、第三方声明和社区文件。
- [x] Issue/PR 模板。
- [x] macOS GitHub Actions Debug、测试、Release、签名验证和 artifact。
- [x] 当前可达 Git 历史的常见私钥/Token 模式检查。

### M-003A macOS 平台目录隔离

- [x] Xcode 工程、源码、资源、测试、第三方许可和构建脚本统一放入 `macOS/`。
- [x] Xcode、命令行构建、GitHub Actions 和文档统一使用新路径。
- [x] 共享协议和仓库级文件保留在根目录，Windows 源码不受影响。

## P0：公开源码前继续完成

### M-004 交接状态机可测试化（已完成）

- [x] 从 App/UI/硬件控制中分离纯交接状态机。
- [x] 时钟、UDP 发送、USB 状态、唤醒和 DDC 操作通过可替换接口注入。
- [x] 使用虚拟时钟覆盖 150 ms 防抖、150 ms 重发、最多 4 次、600 ms 兜底和 6 秒在线窗口。
- [x] 覆盖 USB 等待期间返回、USB 与请求到达顺序互换、确认丢包和对端离线。
- [x] 覆盖重复、过期、乱序、错误 source/target、错误配对码和错误 version。
- [x] 验证 `status_probe/status_response` 使用相同 eventID 且不产生任何硬件动作。
- [x] 测试不访问真实 UDP、USB、DDC 或显示器唤醒接口。

验收：该项完成时的 v1 状态机验证记录保留；当前网络协议已由 DS-007 收敛为 v2-only。

### M-004A 配置迁移失败安全

- [x] 使用显式 `schemaVersion = 2` 文档保存本机显示器配置。
- [x] 兼容 `Displays.Configuration.v1` Codable 数组和 `Display.1` / `Display.2` 旧键。
- [x] 迁移只在新文档成功写入后生效，原 v1 数据和旧键保留。
- [x] 损坏数据、未知版本、编码失败和写入失败会持久进入用户检查安全状态。
- [x] 安全状态统一阻断 USB、DDC、显示器唤醒和网络交接副作用。
- [x] 只有用户在设置窗口成功保存有效配置才清除安全标记。
- [x] 使用隔离 `UserDefaults` 和可替换失败存储覆盖迁移、拒绝、保留和零副作用路径。
- [ ] 真实损坏配置恢复交互以及真实 USB/DDC/唤醒/网络交接均未做实机验证。

验收：自动测试在无硬件环境验证所有失败路径保留原数据并且四类副作用计数均为零；实机验证保持未完成。

### M-004B DS-004 本机多协同配置模型（历史完成，配置格式已由 DS-007 替代）

- [x] 本机配置升级为 `schemaVersion = 3`，随机生成并持久保存 `localEndpointID`。
- [x] 显示器目录与 `collaborationProfiles` 分离；配置支持添加、删除、重命名、排序和多个同时开启。
- [x] 每个配置独立保存对端主机、端口、NFC 配对码、已确认 endpoint/协议版本、显示器输入映射和本机触发设备引用。
- [x] v2 `macInput` 迁入全局 `localInput`，`windowsInput` 按稳定显示器 ID 迁入旧对端配置；旧数据保留。
- [x] 未知版本、重复 UUID/名称、非法范围以及编码、写入、回读失败均持续进入安全状态。
- [x] 设置页提供多配置管理和零副作用本机检测；菜单只为名称、host、端口、配对码和全部显示器映射完整且已开启的配置显示自定义“切换到…”入口，不再显示“切换到本机”。
- [x] USB 学习开始时固定目标配置 ID；回调、候选选择、取消和 30 秒超时不读取当前选择，目标被删除时丢弃迟到结果；学习期间网络、自动 USB 交接、DDC 和唤醒均被阻断，取消或超时保留原绑定。
- [x] DS-005 v2 运行时完成前，多个已开启配置或唯一已开启但缺少 v1 必需字段/USB 引用时暂停旧 v1 自动协同并阻断副作用；唯一完整迁移配置继续兼容 v1。
- [x] C-001 至 C-015 适用的 macOS 路径、0/1/多显示器、孤立映射、重排和 peer identity 变化等待确认均有纯测试覆盖。
- [x] 现有协议 v1 的 17 条消息向量和 16 条状态机向量继续通过；未实现 DS-005 v2 UDP/HMAC。
- [ ] 多配置 UI、菜单名称刷新、真实 USB 学习绑定和真实 DDC 能力提示尚未做实机交互验证。

验收：自动测试不得访问真实 UDP、USB、蓝牙、DDC、唤醒或输入源切换；实机项保持未完成。

### M-005 / DS-011 DDC 后端接口化与原生单后端收敛

- [x] 定义统一的枚举、能力、VCP 读取/写入、错误、取消和后端可用性接口。
- [x] 正式运行路径只实例化 Apple Silicon CoreDisplay/IOAVService 原生后端；原生不可用、枚举、读取或写入失败均明确失败。
- [x] 删除外部进程、可执行路径检测、历史回退选择和相关错误；Intel Mac 明确报告不支持，不以软件调光冒充 DDC。
- [x] 显示器枚举、读写和缓存按稳定逻辑 ID 关联，不依赖后端枚举顺序；旧 selector/index 缓存继续兼容读取。
- [x] 亮度、对比度、音量功能开关在统一服务层阻断相应读写；单台或单项失败不会中止或误操作其他显示器。
- [x] 单项零值合法；同一显示器三项均成功为零时判为不可信遥测，不覆盖缓存；写入成功后才提交缓存。
- [x] 配置安全、USB 学习安全、取消和迟到结果统一阻断 DDC 调用或结果/缓存提交。
- [x] 10 项模拟后端测试覆盖 C-016 至 C-020、C-024、后端回退、取消/迟到结果、稳定 ID 重排和两类安全状态。
- [ ] 尚未覆盖的显示器、线材、扩展坞和 macOS 版本仍需按完整连接路径验证三项原生 DDC 与多显示器失败隔离；Intel Mac 保持明确不支持，不存在回退或后端选择待验项。

### DS-006 公开文档与样例清理

- [x] C-023 关于页面展示 App 图标、公共简介、Bundle 版本和 GitHub 链接，不读取本机配置或触发 DDC、USB、网络与唤醒动作。
- [x] C-024 公开测试改用中性模拟显示器名称，继续验证三项全零为不可信遥测且不覆盖缓存。
- [x] 平台清单和交接记录不再以具体设备品牌描述公开样例或待验硬件。
- [ ] 关于页面的真实界面交互、原生 DDC 真实读数及其他硬件路径仍需另行授权实机验证。

### M-004C DS-004 手动入口与关于页面收尾

- [x] 菜单仅为完整且已开启的协同配置生成使用自定义名称的“切换到…”入口；旧“切换到 Mac/本机”和固定平台入口及其死代码已移除。
- [x] 设置、状态和提示在可使用配置名称时显示自定义名称或“本机/对端”；协议 v1 内部 `mac` / `windows` 角色字段、时序和自动返回本机语义保持不变。
- [x] C-023 使用纯公开 Bundle/静态信息模型自动验证版本、架构、协议和项目链接，不读取本机配置，也没有 UDP、USB、DDC、唤醒或输入源切换依赖。
- [x] C-021/C-022 覆盖多候选等待用户选择，以及取消、30 秒超时、配置删除和迟到结果保留原绑定；学习期间所有自动副作用均被阻断。
- [x] 54 项 XCTest 全部通过，其中配置测试 29 项；协议 v1 的 17 条消息向量和 16 条状态机向量继续通过。
- [ ] 关于页面、菜单名称刷新、链接点击和 USB 多候选选择尚未进行真实 GUI 交互验证；真实 DDC、USB、网络、唤醒及输入源切换未执行。

### M-006 诊断与脱敏

- [x] 增加诊断页面：版本、架构、协议、对端状态、USB 状态、显示器匹配和 DDC 后端能力。
- [x] 日志不记录配对码；IP、路径、UUID 和设备标识按需脱敏。
- [x] 导出或复制诊断前允许用户预览。
- [x] 诊断操作不得触发输入源切换或 USB 交接。
- [x] 协同“检测”具备会话内可复制的分层诊断，可区分监听、发送、零收包、来源端口、event、endpoint、HMAC、时间窗、重放、迟到响应和超时；主机、endpoint、authTag 与配对码不进入导出文本。

### M-007 兼容性与限制文档

- [x] [`COMPATIBILITY.md`](../COMPATIBILITY.md) 写清私有 CoreDisplay/IOAVService API 的系统升级风险。
- [x] 写清 Apple Silicon、Intel、不同线材、扩展坞、转接器和显示器的 DDC 差异，并以完整连接路径作为兼容性单位。
- [x] 区分“编译/签名验证”“GUI 验证”和“真实硬件验证”。
- [x] 提供不包含路径、地址、配对码、显示器 UUID 或 USB 标识的硬件兼容性反馈模板。

## P1：正式 macOS 分发

### M-101 Developer ID、公证与 Hardened Runtime

当前维护者选择以源码和明确标注的 ad-hoc/未公证测试包进行开源分发；本项延期，不阻塞源码或测试包交付，也不得把测试包描述为正式公证版本。

- [ ] 获取并配置 Developer ID Application 身份，但不把 Team ID 或证书写入仓库。
- [ ] 在不破坏 USB、DDC 和登录启动的前提下评估 Hardened Runtime。
- [ ] 使用独立发布脚本完成签名、notarytool 提交和 stapler 验证。
- [ ] CI secret 只保存签名所需加密材料，PR 构建不得访问发布凭据。
- [ ] 在另一台干净 Mac 上验证下载、解压、移动到 `/Applications` 和首次启动。

### M-102 发布自动化

- [ ] 版本号有唯一来源，App、ZIP、tag 和 GitHub Release 一致。
- [ ] 生成 SHA-256 校验和和发布说明。
- [ ] 未公证测试包与正式公证包使用清晰不同的名称。

### DS-022 稳定本地开发签名（自动验证完成，权限实机待验）

- [x] `build-app.sh` 支持通过环境变量选择本机钥匙串中的有效签名身份，不在仓库保存姓名、Team ID、证书、私钥或身份 SHA。
- [x] 未设置环境变量时继续使用 ad-hoc 签名，PR/CI 构建不访问个人签名材料。
- [x] 身份模式在构建前验证签名身份有效，并对 App、打包副本和 ZIP 解压副本同时执行严格验签及非 ad-hoc/TeamIdentifier 检查。
- [x] 文档明确 Apple Development 仅用于同机开发测试，不等于 Developer ID、公证或正式发行签名。
- [x] 文档要求保持 Bundle Identifier 与签名身份稳定，并固定安装到 `/Applications/DisplaySwitcher.app`；不声称清理既有本地网络权限重复记录。
- [x] ad-hoc 与 Apple Development 两种构建模式均成功；App、打包副本和 ZIP 解压副本严格验签通过，完整 XCTest 149/149 通过。
- [ ] 固定替换 `/Applications/DisplaySwitcher.app` 后，本地网络权限是否稳定复用仍需用户实机验证；本任务未修改 TCC、网络或系统权限。

### DS-023 按需详细诊断记录（自动验证完成，GUI 待验）

- [x] “常规”新增全局“详细诊断记录”开关，默认关闭；设置仅保存在本机，不修改配置 schema 或双端协议。
- [x] 关闭时保留“读取成功/读取失败”和“写入成功/写入失败”等操作结果，不记录 DDC 原始事务、输入源 service 轨迹或协同检测轨迹。
- [x] 开启后才记录会话内脱敏详细轨迹；开关任意切换都会清空既有详细记录，避免旧会话与新状态混合。
- [x] 诊断预览始终可用；关闭时只展示配置与能力摘要并明确记录已关闭，不输出先前 DDC、输入源或协同轨迹。
- [x] 显示器页状态收敛为单行用户结果，不再展示 transport、chip、offset、attempts、checksum、IOReturn 或 rebuild 等内部细节。
- [x] 相关测试覆盖默认关闭、持久化、三类记录器门控、切换清空、关闭时诊断导出隔离和简明状态展示；完整 XCTest 153/153 通过。
- [ ] “常规”开关、关闭/开启后的诊断预览和显示器页单行状态仍需用户进行真实 GUI 验证；自动测试未执行网络、USB、DDC、唤醒或输入源动作。

## P2：通用 3.0

### M-201 首次运行向导

- [ ] 引导检测显示器、学习 USB、填写输入源和配置协同。
- [ ] 完成前保持自动切换关闭。
- [ ] 真实输入源测试必须说明影响并由用户主动确认。

### M-202 协议 v2/HMAC（历史完成，v1 兼容路径已由 DS-007 删除）

- [x] 按 `version` 将同一 UDP 端口的 v1/v2 数据报分派到独立解析器和状态机，v1 字段和时序保持不变。
- [x] 使用 CommonCrypto 实现 NFC 输入、PBKDF2-HMAC-SHA256（200000 次）、HMAC-SHA256 规范化认证和常量时间校验。
- [x] 实现 10 秒时间窗、20 秒 nonce 重放缓存、重复/重用区分和完整消息重发缓存。
- [x] 按已确认 `peerEndpointID` 路由到完整且已开启的配置，重复 endpoint 映射安全拒绝。
- [x] DS-005 历史实现曾包含手动定向、单目标自动交接和 3 秒多目标 `input_present` 发现；该多目标自动路径已由 DS-007/DS-008 删除，当前只保留手动定向协同和独立本机 USB 路径。
- [x] DS-005 历史 USB 适配只向状态机提供逻辑 presence；DS-008 后 USB 不再发送 `input_present`，可选联动只向一个明确配置发送不含设备身份的 `wake_display`。
- [x] 配置检测先校验本机完整性，再执行零硬件副作用的 v2 探测和一次 v1 回退识别；endpoint 首次或变化必须用户确认后才能保存。
- [x] 实施当时全部 v1 公共向量（17 条消息、16 条状态机）和 v2 公共向量通过；当前 DS-008 v2-only 合同为 1 条 NFC、4 条认证、20 条消息、6 条状态机。
- [ ] 真实 UDP 双端、USB/蓝牙 presence、显示器唤醒、DDC 和手动/自动交接尚未授权实机验证。

### DS-007 设置界面、DDC 可靠性与 v2-only 收敛（自动验证完成）

- [x] 删除 v1 解析器、状态机、发送、心跳、探测回退和固定平台角色路径；UDP 入口只接受整数 `version = 2`。
- [x] 本机配置升级到 `schemaVersion = 4`；旧配置先保留备份，再生成协同和 DDC 全部关闭的安全默认配置。
- [x] 设置标签统一为“常规、USB 切换、协同、显示器、关于”，移除全局保存/取消并实施即时原子保存、失败回滚和就地错误。
- [x] 协同状态按配置隔离，覆盖未启用、配置不完整、尚未检测、正在检测、无响应、v2 可用、已连接和 6 秒后连接断开。
- [x] 显示器页使用单一控制通道、联动开关和检测/刷新入口；每台显示器只展示三项紧凑 DDC 控制和状态。
- [x] 新显示器六个 DDC 开关默认关闭；托盘只展示同时满足 `enabled` 与 `showInTray` 的项目。
- [x] 连续写入按“稳定显示器 ID + 控制项” latest-wins 合并；同一显示器通道串行，原生句柄失败后失效、重发现并只重试一次。
- [x] 配置变化、显示器重检、取消或安全闸门关闭会清空待写值；迟到结果不更新 UI 或缓存。
- [x] 47 项 XCTest 通过，覆盖 20 条 v2 消息向量和 18 条当前 v2-only 状态机向量、100 次滑杆合并、回退/恢复、v4 失败安全和按配置状态。
- [x] Debug、Release、`build-app.sh`、严格 codesign 和 `contracts/protocol-v2/validate.py` 通过。
- [ ] 六页真实 GUI、浅色/深色、键盘/辅助功能和真实双端 v2 仍待实机验证。
- [ ] 真实 DDC 连续拖动、USB、唤醒和输入源切换未执行。

### DS-008 本机 USB 双向切换（自动验证完成）

- [x] USB 监听改为只消费一个用户明确学习的本机设备，不依赖 endpoint 路由或协同配置。
- [x] 首次观察只建立基线；USB 离开立即按稳定显示器 ID 异步切换本机 DDC，USB 接入只请求本机唤醒。
- [x] 联动协同默认关闭；关闭时 USB 路径零网络，开启时只向一个已确认 v2 配置发送一次认证 `wake_display`，发送失败不阻断或回滚 DDC。
- [x] 合法 `wake_display` 只唤醒本机显示器，不回复、不执行 DDC；网络唤醒与 USB 接入共用 2 秒合并器。
- [x] 本机配置升级到 `schemaVersion = 5`；v4 先备份再原子迁移，保留非 USB 设置与协同配置，新 USB 功能和联动默认关闭。
- [x] USB 设置页提供单设备学习、每显示器目标输入源、USB 开关、联动开关和明确目标配置。
- [x] USB-001 至 USB-016、20 条 v2 消息向量和 6 条 v2 状态机向量均使用模拟接口通过。
- [x] UDP 使用唯一绑定本机 `listenPort` 的 socket 同时收发，配置的 `peerPort` 仅作为目标端口；重配/停止释放该 socket，回复定向收到数据报的来源地址和端口。
- [ ] 真实 USB 学习/插拔、本机唤醒、网络 `wake_display`、DDC 输入源切换和设置页 GUI 仍待授权实机验证。

### DS-009 Apple Silicon 原生显示控制（自动验证完成，部分实机通过）

- [x] 设置页、菜单和映射字段使用已保存名称或系统产品名称，不再把用户可见名称硬编码为“显示器 1/2/3”。
- [x] 同型号显示器使用稳定逻辑 ID 的本机顺序生成中性序号；枚举重排不改变对应关系，也不展示或记录原始 UUID、IORegistry 路径。
- [x] 当前运行时只选择 Apple Silicon 原生 CoreDisplay/IOAVService 后端；原生枚举、读取或写入失败均明确失败，不调用外部进程，Intel Mac 明确显示不支持。
- [x] 已知原生写入正常，保留现有 `0x51`、五次尝试和双写语义；只由现有 latest-wins 协调器合并高频滑杆值，并以模拟回归防止读取修复破坏写入。
- [x] 读取恢复为五次有限尝试，每次清空 response buffer；Type-C/DP Alt 固定使用 `0x51`，确认为内置 HDMI converter 时固定使用 `0`，不在同一操作中盲探多套策略。
- [x] 本机脱敏诊断区分 `typec-dp-alt` / `builtin-hdmi-converter` / `unknown-external`、service 匹配、读写阶段返回类别和重建次数；不显示 UUID、IORegistry 路径或序列号。
- [x] 按 IODisplayLocation 高权重、产品名和序列信息建立一对一显示器/service 匹配，同时识别 `AppleCLCD2`、`IOMobileFramebufferShim` 和兼容 framebuffer；未绑定通信 service 的在线身份可枚举但不可读写。
- [x] Get VCP 回复校验覆盖长度、XOR checksum、来源、载荷长度、回复 opcode、结果码和 VCP command echo，拒绝迟到或错误回复。
- [x] 同一显示器原生传输按 selector 串行，不同显示器互不阻塞；滑杆继续按稳定显示器 ID 与 DDC 项 latest-wins 合并。
- [x] 配置刷新、取消和设置窗口关闭会清空待写并使原生 service 缓存失效；迟到结果不更新 UI 或可信缓存。
- [x] 原生读取严格校验优先；只有全部严格失败均为坏校验和时，才以两次全新请求、除校验和外完全一致且字段/范围合法的回复生成带 `≈` 的弱校验值，其他错误保持失败。
- [x] 设置页按稳定显示器 ID 恢复已启用项目的持久可信缓存；重开窗口或重建表单显示 `≈值`，无缓存保持“—”，同型号显示器不会串值。
- [x] “读取 DDC 参数”只依据亮度、对比度和音量三个可见功能开关选择读取项；隐藏的旧 `readEnabled` 不再阻断原生调用，全部关闭时明确提示且零后端调用。
- [x] USB 与协同输入映射共用可换行标题和固定紧凑输入框，同型号显示器的本机序号不会被输入框截断。
- [x] USB 与协同动态映射行按稳定 display ID 幂等协调；即时保存不再无条件重建显示器页面，20 轮重复重载、重排和重复输入后行数仍严格等于文档唯一显示器数。
- [x] 原生双写聚合保留任一已接受结果，避免 Type-C 输入源切换后链路立即消失使第二次失败覆盖第一次成功；两次均失败仍进入有限重建/重试。
- [x] M4/macOS 27 的通用 endpoint token 分类覆盖 `dispextE` 内置 HDMI 与数字 `dispextN` Type-C/DP；只提取 token，不保存或展示 IORegistry 路径，并继续保留旧 MCDP/Transport 兼容规则。
- [x] 原生读取诊断显示脱敏的 chip、最后拒绝原因、实际 offset 与有限尝试次数；M4 `dispextE` 与明确 MCDP 的芯片地址分离，Type-C/DP 仅在首选 offset 严格失败后进行一次有界策略回退并缓存成功偏好。
- [x] 模拟测试覆盖不同/同型号名称传播、稳定区分、枚举重排、原生失败、高频合并、串行、故障隔离及迟到结果丢弃。
- [x] UDP 发送与接收改为同一个绑定 `listenPort` 的 socket；回复定向原始来源地址/端口，不再建立第二个复用本机端口的出站 socket。
- [x] 已绑定 v2 配置的手动检测定向已确认 peer endpoint；只有首次未绑定检测使用空 target，且响应必须同时匹配 source、target、event 和 HMAC。
- [x] 坏校验和兼容读取最终失败时，设置页可脱敏区分回复不足、两次语义不一致、字段非法、范围非法和传输错误；不记录原始帧或硬件标识。
- [x] 兼容读取被拒绝时只投影两次回复的 DDC/CI 公共语义字段，可区分 source `0x6F`、`0x02`、`0x00` 和其他值并提示疑似帧移位；不自动扫描、重排或接受 `wrong-source`。
- [x] 止损回退将 Native DDC 读写与 service 管理恢复到 `e2d0dd7`；启动、托盘打开、显示器重检和配置重载只恢复缓存，仅设置页“读取 DDC 参数”可发起硬件读取。
- [x] 输入源 VCP `0x60` 从普通 DDC 调节路径分离；每个切换事件按稳定 selector 重新解析短生命周期 `IOAVService`，不复用读取偏好、诊断或 DDC 值缓存，不执行 GetVCP/readback。
- [x] USB、手动和协同均将同一事件的全部目标作为一个批次送入输入服务；不同显示器并发写入，同一 selector 去重并保持串行，且只与该显示器的普通原生 DDC 共用输入优先的窄 I2C 仲裁。
- [x] 用户在最终版本上完成真实多显示器输入源切换验证，确认小米与 Dell 同时切换，不再出现先后串行；这是用户实机结果，不是自动测试。
- [x] 6 项输入源隔离回归覆盖每次重新 resolve、短生命周传输、3 台串行、零读取、失败隔离、缓存不受影响和输入优先；完整 98 项 XCTest 通过。
- [x] 单变量生命周实验在 SetVCP 返回后将当次 transport 异步强引用 1 秒，不阻塞切换线程；每个新事件仍 fresh resolve，默认最多 32 个活动租约并自动清理。
- [x] 7 项输入源专项和 99 项完整 XCTest 通过；租约的保留、到期释放、不同 transport、普通 DDC 隔离及 100 次快速切换有界性均使用可控调度器验证。
- [x] C2C 诊断以单次操作串联目标、候选枚举、匹配理由、会话匿名 service、实际 `IOAVServiceWriteI2C` 帧/返回/耗时和同一 service 的 VCP `0x60` 设备反馈；内核接受与设备执行明确分离。
- [x] 菜单提供“复制输入切换诊断”，只导出公共 DDC 字段、连接分类和会话匿名索引，不导出显示器 UUID、序列号或 IORegistry 路径；多个候选只记录、不自动试写。
- [x] 12 项输入源专项和 104 项完整 XCTest 通过，覆盖实际抵达 write adapter、候选选择证据、多候选零误写、传输结果与设备反馈分离、匿名 ID 重排稳定及诊断脱敏。
- [x] 用户完成 C2C 与 C2DP 各 3 次诊断样本：C2DP 3/3 成功，C2C 2/3 成功；该结果证明所测 C2C 路径可靠性较低，但不是适用于所有线材或显示器的接口规则。
- [ ] 真实 Apple Silicon 多显示器枚举、同型号物理位置对应、连续拖动、读写失败恢复和系统升级兼容性尚未授权验证。

### DS-010 本地网络权限引导（自动验证完成，实机待验）

- [x] 在现有“协同”页顶部增加紧凑的“本地网络权限”模块，不新增设置标签或 App 内权限开关。
- [x] “检测并申请权限”复用现有协同检测与绑定端口 UDP 路径；不使用 Bonjour、组播、旁路连接或硬件动作触发授权。
- [x] 展示模型仅允许“未检测”“协同连接正常”“系统明确拒绝”“连接失败，请检查权限、地址和防火墙”四类状态。
- [x] 超时、认证失败和普通网络错误保持一般失败；当前 BSD UDP 无法可靠证明 TCC 拒绝，因此生产路径不从模糊错误推断“系统明确拒绝”。
- [x] 明确拒绝展示“系统设置 → 隐私与安全性 → 本地网络”文字路径；`NSLocalNetworkUsageDescription` 准确说明检测、协同唤醒和显示器切换用途。
- [x] 11 项相关展示测试和 118 项完整 XCTest 通过；Debug、Release、`build-app.sh` 与严格 codesign 验证通过。
- [ ] macOS 15 及以上首次授权弹窗、允许、拒绝、重新允许，以及错误地址/对端离线/现有防火墙/错误配对码的状态仍需用户实机验证；未重置 TCC 或修改防火墙。

### DS-011 原生 DDC 单后端清理（自动验证完成，实机待验）

- [x] 删除正式 macOS 运行时中的外部 DDC 进程、可执行路径检测、专属错误和历史回退路由；原生失败明确失败。
- [x] 设置页移除后端选择器和冗余的单后端说明；Intel Mac 在原生后端不可用时明确显示不支持。
- [x] 显示器控制与每台显示器的操作按钮收进标题行左右两侧；读取结果独占下一行并自动换行，避免长诊断挤压标题和按钮。
- [x] 本机配置检查弹窗使用可执行的中文提示，不暴露内部枚举名；原生后端可用时不重复展示后端名称。
- [x] 编辑已启用配置时保留并保存有效的部分输入；配置暂时不完整则自动安全停用，补全后由用户重新启用，不再回滚已输入字段。
- [x] 配置文档不再读取或写入历史后端选择字段；旧字段作为未知字段安全忽略，其他 schemaVersion 5 数据保持不变。
- [x] VCP/cache 字段改为平台无关命名，保留既有 `LastValue.stable.*` 键和值，避免清空可信缓存。
- [x] 37 项 DDC、17 项配置相关测试及 122 项完整 XCTest 通过；Debug、Release、`build-app.sh` 和严格 codesign 验证通过。
- [ ] Apple Silicon 原生枚举、显式读取、亮度/对比度/音量写入与输入源切换仍需用户实机回归；本任务未执行硬件动作。

### DS-014 内建 HDMI 原生 DDC 读取收敛（完成）

- [x] 实机确认内建 HDMI 的 offset 0 与 0x51 均只返回非 DDC/CI 数据；停止继续试探 chip、offset、延迟、写周期、回复长度或宽松校验。
- [x] 正式内建 HDMI 读取只执行一次既定 offset 0 严格事务；失败后不探测备用 offset、不进入弱校验、不重建 service，也不覆盖可信缓存。
- [x] 设置页失败状态收敛为“当前连接不支持可靠读取”，有缓存时明确“显示上次可信值”；不再展示 IOReturn、chip、offset、attempts 或原始回复。
- [x] 保留严格 DDC/CI 校验、EDID-like 拒绝、显式连续输入/输出缓冲区和原始回复不出界面的隐私边界。
- [x] Type-C/DP 严格成功路径、其他 Type-C/DP 失败样本、Set VCP 与输入源写入路径均未改变。
- [x] 50 项 DDC 测试和 135 项完整 XCTest 通过；Debug、Release、`build-app.sh` 与严格 codesign 通过。
- [ ] 第二个 Type-C/DP 失败样本后续单独立项诊断，不与内建 HDMI 根因合并；本任务未执行新的硬件动作。

### M-008 / DS-015 IOAVService 拓扑绑定（自动验证完成，实机待验）

- [x] 移除依赖 IORegistry 遍历先后顺序的 framebuffer/service 关联；只使用当前在线 CoreDisplay 的精确 `IODisplayLocation` 与 DCPAV endpoint 拓扑建立一对一绑定。
- [x] Apple Silicon 平台拓扑按当前节点动态解析：内建 HDMI `disp0` 对应 `dispextE`，数字 `dispextN` 只对应同名 endpoint；不按品牌、型号、枚举顺序或历史接口猜测。
- [x] 同型号、重复 endpoint、未知 endpoint 或多 service 歧义均安全拒绝，不把 DDC 操作发送到另一台显示器。
- [x] 每次显式 DDC 读写前重新发现当前 service；重连、接口变化或旧 service 失效后不复用旧 transport、service identity 或读取偏好。
- [x] 未修改 DDC/CI 帧、chip、offset、重试时序、Set VCP、输入源切换、USB、网络或协议。
- [x] 54 项 DDC 专项和 139 项完整 XCTest 通过；Release `build-app.sh`、打包与严格 codesign 验证通过。
- [ ] 小米内建 HDMI 显式读取、两台同型号显示器物理目标、热插拔和 HDMI/USB-C/DP 接口变化仍需用户实机验证；本任务未执行硬件动作。

### DS-017 内建 HDMI 生产读取事务（严格成功已确认，可靠性失败）

- [x] 静态审计当前 BetterDisplay 生产二进制，确认普通 Get VCP 使用标准 `0x51` 请求寻址；`0x50` 是明确的 `ddc2ab` 备用寻址，不用于普通亮度读取。
- [x] 内建 HDMI 改为最多 8 次完整的“单次 WriteI2C → 10 ms → 单次 ReadI2C”事务，严格失败之间等待 5 ms；每次回复缓冲区重新清零。
- [x] Type-C/DP 继续使用已验证的双写、50 ms 与既有 read offset；Set VCP、输入源切换、service 匹配、USB、网络和协议均未改变。
- [x] 严格 checksum 与语义校验保持不变；失败不接受随机数据，也不覆盖上次可信缓存。
- [x] 54 项 DDC 专项和 139 项完整 XCTest 通过；Release `build-app.sh`、打包与严格 codesign 验证通过。
- [x] 用户实机确认小米内建 HDMI 偶尔可在第 4/8 次完整事务得到严格有效亮度回复；同一构建中 Dell C2DP 仍可第 1 次严格读取成功。
- [x] 后续连续 20 次 HDMI 读取全部失败，证明完整事务重试只提高撞到有效帧的机会，并非可靠性根因；本方案不得单独合并。

### DS-018 IOAVService 生命周期稳定化（实机否定，代码已撤回）

- [x] 对照 BetterDisplay 当前生产二进制：能力查询是独立的 MCCS `0xF3/0xE3` 分块事务，不是亮度 Get VCP 的必要前置；应用使用全局串行 DDC 队列并长期持有同一 `IOAVService`。
- [x] 根因审计发现 DS-015 虽在每次操作前正确重验当前拓扑，却也在每次重验时调用 `IOAVServiceCreateWithService` 并替换对象；这是当前与 BetterDisplay 最关键的生命周期差异，也符合“退出重开偶尔恢复、同一连接随机撞到有效帧”的实机现象，是否为根因由本轮实机验证决定。
- [x] 每次显式硬件操作仍重新枚举在线显示器及当前 registry 拓扑；只有 selector、registry service identity、transport、chip 和在线状态均未变化时复用现有 service，且枚举位置变化不使稳定 service 失效。
- [x] 热插拔、接口/transport/chip 变化、service identity 变化、离线或既有恢复路径主动失效时不复用，按当前拓扑创建新 service；不按品牌、型号、端口顺序或历史接口猜测。
- [x] 未改 DDC 帧、offset、重试次数、等待时间、严格校验、Set VCP、输入源切换、USB、网络或共享协议。
- [x] DDC 专项 XCTest 55/55，完整 XCTest 140/140；Release `build-app.sh`、严格 codesign 与 ZIP 完整性验证通过。
- [x] 用户实机连续读取小米内建 HDMI 20 次均失败，同一构建中 Dell C2DP 保持严格成功；service 生命周期不是该故障根因。
- [x] DS-019 已撤回全部 service 复用代码和对应测试，恢复每次操作按当前拓扑创建 service；DS-018 不进入合并候选。

### DS-019 内建 HDMI Get VCP 请求校验和（完成）

- [x] 根因审计发现旧公开 AppleSiliconDDC 对单字节 Get VCP 请求漏算写入目标地址 `0x51`：亮度请求被构造成 `82 01 10 FD`，而标准帧应为 `82 01 10 AC`。
- [x] 既有 HDMI 诊断中被拒绝回复的 `payloadLength=0x82 / opcode=0x01 / result=0x10 / command=0xFD` 与错误请求帧完全对应，证明驱动读回了未被显示器接受的本机请求，而非有效 DDC/CI 回复。
- [x] 静态审计 BetterDisplay 当前生产二进制，确认其普通 Get VCP 校验和同时异或 chip 写地址与 `0x51`；其读取地址和完整事务结构与前序审计一致。
- [x] 仅内建 HDMI Get VCP 改为包含 `0x51` 的校验和；已实机稳定的 Type-C/DP 单字节读取继续使用既有兼容帧，所有 Set VCP 写入继续包含 `0x51`，输入源、USB、网络和共享协议均未改变。
- [x] 纯帧测试固定 HDMI Get VCP=`82 01 10 AC`、Type-C/DP Get VCP=`82 01 10 FD`、Set VCP 亮度 100=`84 03 10 00 64 CC`。
- [x] DDC 专项 XCTest 56/56、完整 XCTest 141/141；Release `build-app.sh`、严格 codesign 与 ZIP 完整性验证通过。
- [x] 用户完全退出 BetterDisplay 后实机确认：小米内建 HDMI 连续 20/20 次严格成功，Dell C2DP 连续 3/3 次严格成功；首张截图两者均为 `attempts 1`，无读取回归。

### DS-020 扩展坞 HDMI Get VCP 校验策略（完成）

- [x] 第二台显示器经 USB-C 扩展坞 HDMI 暴露为 `typec-dp-alt`，但失败回复中的 `payloadLength=0x82 / opcode=0x01 / result=0x10 / command=0xFD` 与旧 Get VCP 请求帧一致；链路分类不能单独决定请求校验方式。
- [x] Type-C/DP 首选当前已验证策略；严格失败后才以失败地址为起点尝试另一种校验和，任一严格成功立即停止，不按品牌、型号、显示名称或枚举序号分支。
- [x] 成功的 offset 与校验策略按 selector + 当前 service identity + transport 缓存；重新连接、换接口、service 变化或取消时失效，避免把扩展坞策略套到另一条链路。
- [x] 诊断新增脱敏的 `checksum legacy/standard`，用于区分实际成功策略；不展示 UUID、IORegistry 路径或请求原始字节。
- [x] 写入、输入源切换、USB、网络、共享协议和内建 HDMI 的标准校验路径均未改变。
- [x] DDC 专项 XCTest 60/60、完整 XCTest 145/145；Release `build-app.sh`、App 与 ZIP 解压后严格 codesign、ZIP 完整性验证通过。
- [x] 用户实机确认三条链路同时正确：内建 HDMI 使用 `standard` 第 1 次严格成功，直连 C2DP 使用 `legacy` 第 1 次严格成功，扩展坞 HDMI 首次经 11 次事务切到 `standard` 严格成功。
- [x] 用户继续读取扩展坞 HDMI 后确认只有首次较慢，后续明显加快，证明 selector + service + transport 的 offset/校验策略缓存生效；没有发现已成功链路回归。

### DS-024 托盘空显示器分组清理（自动验证完成，GUI 待验）

- [x] 托盘菜单先投影同时满足功能启用与“在托盘显示”的显示器控制项；没有任何可见控制项的显示器不创建标题或空子菜单。
- [x] 投影按稳定显示器身份关联运行配置和持久配置，不按品牌、型号、显示器数量或枚举顺序特判。
- [x] 有可见控制项的显示器继续保留名称、滑杆和值；联动、协同菜单、设置和退出行为不变。
- [x] 托盘静态动作只保留“设置…”和“退出”；“联动调节所有显示器”“重新检测显示器”和“查看诊断预览…”仅移除托盘入口，设置页对应功能保留。
- [x] 滑杆联动目标直接读取持久配置 `linkAllDisplays`，不再把隐藏菜单项状态作为业务真值；设置页即时保存继续生效。
- [x] 动态协同与显示器区域为空时隐藏其分隔线，不产生首部、尾部、孤立或重复分隔线。
- [x] 纯模型回归覆盖零可见分组、部分显示器可见，以及分组只包含同时满足两个开关的控制项。
- [x] 纯模型继续覆盖静态动作、动态分隔线和持久联动目标；完整 XCTest 155/155、Release `build-app.sh`、App 与 ZIP 解压副本严格 codesign 验证通过。
- [ ] 真实菜单中全部关闭、部分显示器开启及多控制项组合仍需 GUI 验证；未执行真实 DDC、USB、网络、唤醒或输入源动作。

### DS-025 USB 与协同设置布局对齐（自动验证完成，GUI 待验）

- [x] USB 页面收敛为“自动切换”“联动协同”两组；自动开关、触发设备、当前状态与对端输入源映射位于同一卡片，联动目标与开关保持独立卡片。
- [x] 协同页面收敛为“协同状态”“配置”两组；当前配置选择与配置详情位于同一卡片，网络权限与连接检测保持独立状态卡片。
- [x] 两页使用 AppKit 可滚动内容区；0、1、2、3 台及更多显示器使用稳定显示器 ID 生成唯一映射行，同型号区分后缀完整保留。
- [x] 输入源字段保持固定紧凑宽度，长显示器名称优先获得空间；按钮、输入框和动态映射补充 VoiceOver 标签，深浅色继续使用系统语义色。
- [x] 实机反馈发现横向行沿用 `NSStackView.gravityAreas`，低 hugging 的空视图不会获得剩余宽度；现统一使用 `.fill`，以真正的可扩展间隔或主控件把添加、学习、联动开关及同类尾部控件固定到卡片右缘。
- [x] 第二轮实机反馈发现卡片与页面语义色在浅色下近似、深色下又无边框与裁切，导致层级和圆角不可辨；现由共享 AppKit 卡片/页面组件统一使用 `controlBackgroundColor`、`underPageBackgroundColor`、`separatorColor`、连续圆角和裁切，并在 appearance 变化时刷新。
- [x] 第三轮实机反馈发现窗口外层与标签页内层同时绘制背景，四周露出直角大矩形；现将窗口内容区作为唯一画布背景，普通页、滚动页、显示器页、诊断页和关于页的 page/scroll/document 承载层均保持透明，卡片继续保留独立语义层级。
- [x] 第四轮实机反馈发现“显示器控制”卡片顶部出现孤立横线；根因是模块标题在卡片外部而内容数组首项仍是分隔线。现仅移除该卡片首个分隔线，保留每台显示器读取状态与控制区之间的有效分隔线。
- [x] 第五轮实机反馈发现协同“配置详情”表单标签比对端输入源列表右缩；根因是表单 label 使用右对齐固定列而映射区直接从卡片内容左边开始。现统一表单 label 与卡片内容左基线，控件起始列继续由共享固定列保持稳定。
- [x] 删除“配置详情”卡片底部无用的“上移/下移”按钮、布局占位和展示 action；保留“删除配置”、自动保存状态、配置数据顺序兼容和底层持久化格式。
- [x] 第六轮实机反馈发现“对端输入源”作为独立标题仍割裂表单结构；现改为左标题列 + 右映射列表的两列结构，标题按右侧列表容器 centerY 垂直居中，0/1/3+ 行均不按行数手算偏移。
- [x] 第七轮视觉反馈发现保存状态放在配置详情操作行里且成功也使用红字，容易误解为错误。现复用同一 `peerSaveStatusLabel` 和 `persistDocument` 结果，将唯一保存状态迁移到协同页底部；成功显示系统 checkmark 图标与次级文字“已保存”，失败显示红色错误图标与“保存失败，已恢复”。
- [x] 第八轮复查发现上一轮“底部”仍是协同页滚动内容末尾，不是窗口固定状态栏。现把保存状态迁移到窗口级非滚动 footer，`tabView` 在 footer 上方结束；滚动内容不再包含保存状态，协同页任意滚动位置都应可见。
- [x] 第九轮实机反馈发现固定 footer 在打开页面和切换配置后始终显示“已保存”，缺少真实保存事件，容易被理解为静态假状态。现初始和重载均隐藏，切换标签/配置只清除短暂成功态；只有 `persistDocument` 真实成功后才在左侧以绿色图标和文字显示约 2 秒，连续保存重置同一隐藏计划；失败跨标签/配置切换保持红色直到下一次成功，窗口关闭安全取消待处理任务。
- [x] 第十轮审核发现 `persistDocument` 同时承载 USB、显示器与协同写盘，第九轮无条件记录结果会把非协同成功/失败带入协同 footer。现持久化调用显式携带反馈 scope 且默认不反馈，只有协同 profile 编辑/启用、添加、删除和确认 endpoint 才进入协同保存状态机；不依赖当前标签猜测来源。
- [x] 第十一轮实机反馈发现 USB 映射仍以卡片外部“对端输入源”标题 + 整卡宽列表展示，与协同配置详情的左标签/右列表结构不一致。现移除 USB 外部重复标题，两页共同复用 `labeledVerticalControlRow` 与 `SettingsMappingListLayout` 的 90/490 两列合同；左标签按完整列表容器 `centerY`，0 台显示安全空态，1/3+ 台不手算偏移。
- [x] 第十二轮结构反馈要求减少碎片卡片。USB 把对端输入源并入自动切换卡片，协同把当前配置与配置详情并入“配置”卡片；生产展示模型同步由三组收敛为两组，并以显式 separator row 描述卡片内部边界，不保留旧分组投影。
- [x] 第十三轮安全复查发现映射完整性要求每台显示器都有输入源，并且整数校验接受 `0`；空字段因此被迫转换为危险哨兵。现 USB 与协同映射允许留空并按显示器跳过，至少一台存在 `1...65535` 映射才允许启用；USB 的 nil、0、负数或越界映射均按现行公共合同只报告 `missing_mapping` 且零硬件/网络副作用。旧配置中的 `0` 在加载时归一为缺失，最终输入源服务、DDC 路由和原生写入口均有独立非零范围门控。
- [x] 第十四轮为 USB 增加与协同同源但按 scope 隔离的窗口固定保存反馈。只有 USB 实际配置变化且原子持久化成功才显示左侧绿色“已保存”约 2 秒，连续保存重置计划；失败持续到下一次 USB 成功。USB、协同与其他设置的成功/失败不会互相覆盖或清除。
- [x] 配置名称字段此前与弹性空白并列，空白吸收剩余宽度后字段只保留固有内容宽度。现统一为 90 点标签列 + 490 点右控制列；字段在右列内弹性填满，启用开关固定尾部且不再位于列外竞争宽度。当前配置下拉框与添加按钮复用同一合同。
- [x] 普通动作按钮统一为 AppKit regular/rounded 样式和最小高度，覆盖学习、权限/连接检测、配置管理、显示器刷新/读取及诊断刷新/复制；链接、开关、下拉框和删除确认语义不变。
- [x] 协调端复查发现既有输入源并发测试错误固定 resolver 调用顺序，170 项完整测试存在间歇失败；现修正为验证 selector 集合完整和结果归因稳定，不把不同显示器并发路径改回串行。
- [x] 即时原子保存、失败回退、USB 学习、网络检测及零硬件副作用语义保持；输入源启用条件按新安全合同收敛为“至少一台有效映射”，空映射只跳过对应显示器。DS-023 诊断门控和 DS-024 托盘收敛继续通过回归。
- [x] USB-001～016 公共向量不经筛选逐条通过，完整 XCTest 194/194 通过；`build-app.sh`、Apple Development 严格 codesign、ZIP 完整性及解压副本验签通过。
- [ ] 窄窗口、0/1/2/3+ 台真实显示器、浅色/深色、键盘导航与 VoiceOver 仍需 GUI 验证；未执行真实 DDC、USB、网络、唤醒或输入源动作。

### DS-027 联动 DDC 统一控件（自动验证完成，GUI/实机待验）

- [x] 根因确认：原 `linkAllDisplays` 只改变单个滑杆事件的写入目标，设置页和托盘仍始终按显示器投影，因此联动开启后继续显示重复滑杆。
- [x] 联动关闭时保留原逐显示器滑杆与托盘分组；联动开启时设置页只投影亮度、对比度、音量各一个公共滑杆，每台显示器的功能开关、托盘开关、读取按钮和状态不变。
- [x] 公共值只显示全体相同值、`混合` 或 `—`，不计算平均值也不冒用单台值；辅助功能值使用相同语义。
- [x] 审核修复：`混合`/`—` 使用无拇指的中性轨道，避免默认值或旧值的拇指冒充全体值；设置页与托盘复用同一 AppKit 滑杆状态，首次真实鼠标/键盘调节才恢复具体拇指并提交绝对值。逐显示器滑杆继续使用原生确定值控件。
- [x] 公共最大值取所有在线唯一目标有效上限的最小值，未知上限使用安全默认 100；公共写入把同一绝对值提交给所有启用该功能的在线唯一目标。
- [x] 托盘联动模式按控制项扁平投影，不创建显示器分组；任一已配置显示器同时启用功能与托盘显示即可显示公共项，但实际写入仍只面向全部启用该功能的在线唯一目标。
- [x] 最后一次成功检测的运行时拓扑与磁盘配置分离；设置保存、磁盘重载、检测进行中或失败都不会把离线磁盘条目伪装为在线写入目标。
- [x] 继续复用既有 DDC latest-wins、按目标取消、代次、部分失败隔离及安全门；切换联动或显示器功能/托盘开关后设置页与托盘立即重建，配置 schema 不变。
- [x] 纯投影与 fake 写入测试覆盖联动开关、0/1/多显示器、同值/混合/未知、功能/托盘组合、离线/歧义过滤、安全最大值、扁平托盘、绝对值批量写入、部分失败及中性轨道首次交互转换；定向 68/68、完整 XCTest 203/203 通过。
- [x] Release `build-app.sh` 使用本机有效 Apple Development 身份完成；输出 App、ZIP 与解压副本严格 codesign 和 ZIP 完整性验证通过。
- [ ] 真实 0/1/多显示器、不同 DDC 上限、浅/深色、VoiceOver、托盘与设置页即时重建，以及部分显示器写入失败仍需 GUI/硬件验证；未执行真实 DDC、USB、网络、唤醒或输入源动作。

### DS-028 托盘与设置窗口生命周期（自动验证完成，GUI 待验）

- [x] 托盘顶部使用只读语义状态，仅按持久化 `usbSwitch.enabled` 显示“USB 切换已开启”或“USB 切换已关闭”；不投影触发设备名称、VID/PID、路径或其他硬件身份，学习/安全执行门不会把已开启设置冒充为关闭。
- [x] 逐显示器子菜单与联动扁平项共用 252 点单行 DDC 行：语义图标、名称、滑杆和值；保留确定值、混合、未知及首次交互语义，不改变写入目标和批处理行为。
- [x] USB 状态、协同切换、显示器分组、DDC 控件、设置和退出均使用 SF Symbols 语义图标；符号不可用时安全退化，辅助功能标签保留完整动作含义。
- [x] 左键与右键均只打开托盘菜单；只有明确选择“设置…”才打开设置窗口，不存在单击状态栏图标直接打开设置的 action。
- [x] 打开设置切换为 `.regular`、显示 Dock 图标、激活已有窗口并使其成为 key/main；窗口保持普通层级、切换其他 App 不隐藏或销毁，关闭后回到 `.accessory` 且状态栏进程继续运行。
- [x] 纯模型与 AppKit 合同覆盖 USB 精确文案、全部图标、菜单宽度和单行布局、左右键路由，以及 regular/accessory、normal level、不随停用隐藏和可复用窗口语义。
- [x] 原 DS-028 定向 XCTest 124/124、USB 状态语义定向 XCTest 32/32、最新完整 XCTest 221/221 通过；Release 构建与严格验签结果见本任务交接记录。
- [ ] 真实托盘宽度、长中文显示器名称、鼠标左右键、键盘滑杆、VoiceOver、Dock 图标切换及切换其他 App 后窗口保留仍需 GUI 验证。

### DS-029 离线显示器安全手动删除（自动验证完成，GUI 待验）

- [x] 检测只更新在线/离线，不再用当前枚举结果覆盖并自动删除磁盘中的未出现显示器；运行时 DDC 目标仍只包含最后一次成功检测到的在线显示器。
- [x] 删除资格同时要求 CG 外接身份与 IOKit 外部 DCPAV/IOAV 物理服务一一完整匹配，再连续两次未发现同一已保存显示器；虚拟/未解析身份、CG 漏掉物理服务、身份抽取不完整、IORegistry 失败、检测中/失败/空结果/重复 selector、首次缺失或当前在线均不显示删除按钮，并清空连续缺失证据。
- [x] 删除前显示破坏范围确认；取消不产生变更。确认后一次原子写盘同时移除显示器配置、USB 映射、所有协同映射与该稳定身份的本机 DDC 缓存/托盘偏好，保存失败保留原文档并进入既有安全反馈。
- [x] 删除后复用现有完整性规则：USB 无有效映射时关闭；协同无有效映射时停用；仍有部分有效映射时保持启用，不要求所有显示器都有映射。
- [x] 缓存清理只使用 stable ID 与 selector 键，不删除基于枚举序号的历史键，也不影响其他同型号显示器。删除成功后的运行时刷新只更新内存安全投影，不重启 USB 监听、停启网络监听、发送探测或执行 DDC/输入源动作。
- [x] 自动测试覆盖本机物理拓扑一一完整性、虚拟身份、CG 漏枚举物理服务、身份抽取不完整、可信离线判定、在线/检测中/失败/不可信拒绝、取消、级联映射、部分映射保留、最后映射安全停用、原子保存失败回滚、离线配置保留、身份不猜测及缓存隔离。
- [x] DS-029 DDC/配置定向 XCTest 95/95、最新完整 XCTest 221/221 通过；Release 构建与严格验签结果见本任务交接记录。
- [ ] 真实断开/重接显示器、两次可信检测、确认/取消、浅深色及保存失败 UI 仍需 GUI 验证；未执行真实 DDC、USB、网络、唤醒或输入源动作。

### DS-030 macOS 托盘图标完整性与媒体键审计（图标自动验证完成，快捷键待定）

- [x] 根因确认：状态栏按钮仍直接使用 `display.2`，没有采用双端统一的“显示器 + 百分号”图形；菜单项各自直接请求单个 SF Symbol，缺少统一尺寸、模板属性和最低系统兼容候选，真实菜单可能出现图标缺位或视觉重量不一致。
- [x] USB 状态、协同切换、显示器分组、亮度、对比度、音量、设置和退出全部经过同一菜单图标工厂，统一为 16 点、regular weight、template image；每种语义提供按顺序选择的 SF Symbol 候选，保留完整辅助功能描述。
- [x] 用户在 macOS 27.0（26A5421a）的实拍确认：自定义亮度/音量行图标正常，但 USB 状态、协同切换、设置和退出等标准 `NSMenuItem` 图标仍被系统隐藏。根因不是 image 缺失，而是 macOS 27 起 `preferredImageVisibility` 默认 `.automatic`，AppKit 通常不显示菜单项 image；`TrayImageFactory.apply` 现于 macOS 27 及以上显式设为 `.visible`，旧系统由 availability 分支保持原行为。显示器子菜单同样经过该生产入口，自定义滑杆行继续拥有独立 `NSImageView`。
- [x] 状态栏图标改为代码绘制的透明单色 template image，只影响菜单栏状态项；18 点画布中主体占 90%，线宽为画布的 6%，AppKit 自动适配浅色/深色菜单栏。Dock、应用与设置窗口继续使用彩色 AppIcon。
- [x] 图标合同测试除 image/template/尺寸外，在 macOS 27 真实执行环境逐一断言六种生产标准角色经 `apply` 后 `preferredImageVisibility == .visible`；DS007 定向 XCTest 35/35、完整 XCTest 271/271 通过。Release arm64 使用 Apple Development 签名，App 与 ZIP 解压副本严格验签，ZIP 完整性及 arm64 架构通过；测试包 SHA-256 `7788948603dd488df26f79362cf7892c2ce7bcaf8f71c4dd7cbab33837313d77`。
- [x] 媒体键现状审计确认当前工程没有全局键盘监听、媒体动作解析、权限 UI 或设置项；因此“快捷键”此前只停留在讨论，尚未落实。
- [x] 后续实现必须消费 macOS 已解析的媒体动作，而非普通 F 键码：亮度减/加对应 F1/F2，静音/音量减/音量加对应 F10/F11/F12；用户开启“将 F1、F2 等键用作标准功能键”后，由 Fn 组合产生相同媒体动作，不单独监听 Fn。
- [ ] 暂不启用全局媒体键监听，不申请辅助功能/输入监控权限，不吞系统事件，也不发送 DDC；需先明确联动关闭时控制哪台显示器、系统原生亮度/音量是否同步，以及 F10 静音的恢复值/显示器能力语义。
- [ ] 新包仍需用户在 macOS 27 实机复核 USB 状态、协同切换、显示器子菜单、设置和退出图标确实渲染，并覆盖 1x/2x Retina、浅色/深色菜单栏；18 点状态图标也仍需真实菜单栏视觉验证。未执行真实 DDC、USB、网络、唤醒或输入源动作。

### DS-031 macOS 原生媒体键关联 DDC（亮度/音量写入实机通过，接管与 UI 待验）

- [x] 使用只读 `CGEvent` session event tap 监听系统最终产生的 `NX_SYSDEFINED` 辅助控制动作，只归一化亮度减/加、静音、音量减/加；不匹配普通 F 键码、不单独监听 Fn，回调始终返回原事件，系统原生行为继续执行。
- [x] 权限入口使用 `CGPreflightListenEventAccess` / `CGRequestListenEventAccess`，只申请输入监控而非辅助功能权限；未授权或监听创建失败时零 DDC 写入，常规、托盘、USB、协同和手动 DDC 不受影响，常规页给出明确状态与系统设置路径。
- [x] 联动关闭时，对已启用对应 DDC 项的可信在线物理显示器分别按 5 步进，在可信当前值基础上保持各自差值；未知、估算、离线、虚拟、RDP 类或不完整拓扑目标不猜值、不写入。
- [x] 联动开启时，只有全部目标存在可信且相同的当前值时才生成同一绝对值；混合或未知状态整次零写入，不退化为相对值。长按重复事件复用既有 per-display latest-wins、generation、取消与安全门，并使用仅从可信样本起步的会话投影连续推进。
- [x] 静音只作用于已启用音量的可信目标；会话内按 stable ID 保存最近非零值，写 0 后再次按键恢复。重复静音事件忽略，未知/不支持/无安全恢复值时零写入，恢复值不持久化且在检测、配置重载、安全状态及退出时清除。
- [x] 修复冷启动只恢复 `estimated` 缓存、媒体键路由因而拒绝全部目标的运行时根因：每批媒体动作先在串行 worker 上对完整可信 CG/IOAV 一一匹配目标执行有界新鲜 DDC 读取，只有非估算成功值进入路由。非联动跳过单台读取失败，联动任一读取失败或值混合则整组零写入；读取中只与连续队尾同动作 repeat 合并，不同动作进入最多八批的 FIFO 队列，当前批完成路由并提交写入后才启动下一批读取；repeat 或队列达到上限时明确拒绝并显示诊断。配置/拓扑 generation、安全门或退出变化会清空整队并作废迟到结果，写入仍统一进入 latest-wins coordinator。
- [x] 诊断增加脱敏阶段 `event-seen`、`fresh-read-started`、`fresh-read-failed`、`route-blocked`、`write-submitted`，用于区分系统是否产生媒体事件、读取失败和安全阻止；仍只监听 `NX_SYSDEFINED`，不增加普通 F/Fn 键监听。
- [x] USB 开关状态、协同切换、显示器子菜单、亮度/对比度/音量自定义行、设置和退出均使用生产菜单角色投影；标准菜单项必有 16×16 template image，且 macOS 27+ 显式请求 `preferredImageVisibility = .visible`，避免系统 `.automatic` 策略隐藏已有 image。自定义行拥有独立 `NSImageView`，SF Symbol 不可用时使用确定性单色自绘图标。
- [x] fake 测试覆盖五类媒体动作、冷启动首次事件、新鲜读取成功/失败/部分成功、`up/down/up` 边界顺序、亮度/音量/静音 FIFO 不丢事件、下一批等待当前路由确认、连续 repeat 合并与显式容量上限、generation 整队取消、联动混合/未知、非吞事件、离线/虚拟/RDP 类不可信拓扑、静音存取、生产菜单图标和零真实硬件副作用；媒体键定向 XCTest 25/25、完整 XCTest 248/248、Apple Development Release arm64 构建、App 与 ZIP 解压副本严格验签及 ZIP 完整性均通过。
- [x] 实机确认 F1/F2 已正确调节外接显示器 DDC 亮度，F10/F11/F12 也已实际改变显示器 VCP `0x62`；原生音量 OSD 仍出现的根因不是 DDC 读写失败，而是原事件 tap 明确使用 `.listenOnly` 并始终放行系统事件。
- [x] 新增默认关闭、向后兼容的“HDMI/DP DDC 音量接管”本机 opt-in。CoreAudio 只使用公开 `AudioObject` 读取和监听默认输出、alive、精确 HDMI/DisplayPort transport、输出声道及系统 volume/mute 可写性；任何属性或监听不完整均 fail-closed。启用后，非 HDMI/DP 或系统可调输出纯交给 macOS 且零 DDC，符合条件 HDMI/DP 在权限不足时仍放行并执行 DDC；关闭时保留既有 listen-only + DDC 行为。
- [x] 只有辅助功能已授权、完整可信物理拓扑、全部在线 `volumeEnabled` 目标、非估算 `0x62` 新鲜样本以及配置/DDC/音频 generation 全部一致，且整批 DDC 写成功后才 session armed。首次未 armed 按键继续交给 macOS 并照常走 DDC；armed 不因普通闲置超时自动解除，直到输出/拓扑/目标/配置/generation/权限/opt-in/tap 或读写状态变化。每个已吞事件仍必须先完成与当前动作绑定的全目标新鲜读取，证据生成当前批次后即清除、不可跨事件复用；读取失败整批零写、一次失败 HUD 并立即 disarm。active tap 只吞 F10/F11/F12 的 down/repeat 与配对 key-up，F1/F2 和其他事件永远放行，不申请 Post Event、不重放合成事件。
- [x] 使用公开 AppKit 的非激活、忽略鼠标、跨 Space borderless `NSPanel` 显示“DDC 音量”和成功提交值/百分比；边界无写入时只标记“当前读取”，不冒充硬件回读或成功写入。常规页提供开关、辅助功能授权入口、被动/主动、音频路由和 armed 状态；诊断仅输出脱敏 transport/可写性、pass-through/consumed 与 DDC submitted/succeeded/failed。
- [x] 修复常规页权限区只隐藏按钮但保留固定高度容器的布局根因：输入监控与辅助功能分别保留自己的完整状态行，权限动作只作为行内 arranged subview 动态加入/移除；权限已满足时不留下空动作行或孤立分隔，状态变化立即重新布局。两种权限文案、按钮和 VoiceOver 分组保持独立，普通动作按钮统一 regular/rounded。
- [x] 根据实机对比修复 HUD 呈普通浅色卡片的根因：原先 `.regular` 玻璃嵌套手工描边/裁剪 wrapper，形成重复背景和压平玻璃观感。macOS 26 及以上现直接以公开 `NSGlassEffectView.Style.clear` 作为唯一材质根视图，由系统玻璃自身承担圆角与背景合成；内容层透明、无手工重描边、无外层裁剪，只保留面板柔和阴影。macOS 12–15 回退单层、透明度更合理的 `NSVisualEffectView.Material.popover`。窗口仍按鼠标所在屏幕、无匹配屏幕时回退主屏，锚定 `visibleFrame` 右上角并保留安全边距；保持非激活、忽略鼠标、跨 Space/全屏，连续按键复用同一窗口并重置淡出版本，旧计时不能关闭新内容。
- [x] tap timeout/user-disable 在 event-tap callback 线程先同步锁内清空消费快照和残留 key-up ownership，再异步由主线程 disarm/切回 passive；active tap 不在安全状态更新前自行重启，同模式监听不重复重建。
- [x] 新增 fake 测试覆盖权限、HDMI/DisplayPort 与系统可写性、输出切换、audio generation、首键放行后 armed、全目标写完成、sound down/repeat/配对 key-up、F1/F2 放行、F10 恢复、非显示/系统可调输出零 fresh read/write、tap-disable 返回前同步 fail-open、一小时模拟闲置后仍接管但下一动作必须新鲜读取、旧事件证据不可复用，以及权限行完整性/两种权限独立、HUD `clear` glass/旧系统 fallback、单一直接材质层、透明内容、无手工重描边/双重背景、多屏右上角、连续更新/失败/非激活行为、macOS 27 标准菜单项显式图标可见性和零真实硬件副作用；媒体键/HUD 定向 XCTest 47/47、DS007 定向 XCTest 35/35、完整 XCTest 271/271 通过。Release arm64 使用 Apple Development 签名，输出 App 与最终 ZIP 解压副本均严格验签，ZIP 完整性和 arm64 架构通过；测试包 SHA-256 `7788948603dd488df26f79362cf7892c2ce7bcaf8f71c4dd7cbab33837313d77`。
- [ ] 仍需实机验证：首次开启/关闭 opt-in、辅助功能拒绝/允许/撤销，内建扬声器/耳机/USB/Bluetooth/AirPlay 与 macOS 可调 HDMI 的原生行为，符合条件的 HDMI/DP 首键放行及后续 F10/F11/F12 吞键、长按、切换默认输出；权限行在权限切换、浅/深色与 VoiceOver 下的紧凑布局，以及 macOS 26 `clear` 液态玻璃、旧系统回退、多屏右上角、失败、连续按键、跨 Space/全屏 HUD。公开 AppKit 只能接近系统材质，不能像素级复制私有控制中心；没有公开稳定 API 将 CoreAudio 输出映射到某一 CGDisplay，因此接管目标严格使用用户显式启用的全部在线 DDC 音量目标，不猜测映射。
- [ ] 仍需用户在输入监控允许/拒绝/重新允许、标准功能键开关两种模式、长按重复、联动开关、混合/未知值及真实外接显示器上验证；自动验证未执行真实 DDC、USB、网络、唤醒或输入源动作。

### M-203 Bonjour/mDNS 自动发现

- [ ] 在协议 v2 安全模型确定后实施。
- [ ] 自动发现只列出候选设备，不自动信任、配对或触发硬件动作。

## 进度记录

| 日期 | 清单项 | 状态 | 提交 SHA | 验证说明 |
| --- | --- | --- | --- | --- |
| 2026-08-27 | M-002 动态多显示器 | 完成 | 1229b9b | 6 项配置与迁移测试通过 |
| 2026-08-27 | M-003 开源仓库基础 | 完成 | f46eb28、0a1ee8c | 本地与 GitHub Actions 均通过 |
| 2026-08-27 | M-003A macOS 平台目录隔离 | 完成 | 1a0faab | Debug、Release、13 项测试、脚本打包、严格验签和 GitHub Actions 通过 |
| 2026-08-27 | M-004 交接状态机可测试化 | 完成 | 39e6298 | PR #4 已合并；17 条消息向量、16 条状态机向量和 macOS CI 通过 |
| 2026-08-27 | M-004A 配置迁移失败安全 | 自动验证完成，实机待验 | 6ac7c92 | 16 项配置测试、29 项全部 XCTest、Debug/Release、打包与严格验签通过 |
| 2026-08-27 | M-004B DS-004 本机多协同配置模型 | 自动验证完成，实机待验 | 26c1e93、a6aeb21、9ccf847、7581b05 | 27 项配置测试、40 项全部 XCTest、Debug/Release、打包与严格验签通过；30 秒 USB 学习超时及学习期间零自动副作用已覆盖，PR #20 CI run 33088068267 通过 |
| 2026-08-28 | M-005 DDC 后端正式接口化 | 自动验证完成，实机待验 | 7f08166 | 10 项 DDC 测试、50 项全部 XCTest、Debug/Release、通用架构构建、打包与严格验签通过；PR #23 CI run 33093199681 通过 |
| 2026-08-28 | DS-006 公开文档与样例清理 | 自动验证完成，实机待验 | 55d933b | C-023 关于页代码审计、50 项 XCTest、Debug/Release、打包与严格验签通过；C-024 使用中性模拟显示器名称；PR #26 CI run 33099308922 通过，真实硬件与 GUI 交互待验 |
| 2026-08-28 | M-004C DS-004 手动入口与关于页面收尾 | 自动验证完成，GUI/实机待验 | dc0c302 | C-021 至 C-023、29 项配置测试、54 项全部 XCTest、Debug/Release、打包与严格验签通过；PR #29 CI run 33103309304 通过，真实 GUI 与硬件路径未执行 |
| 2026-08-28 | M-202 DS-005 协议 v2/HMAC | 自动验证完成，双端实机待验 | 0d40c1f | 63 项 XCTest、v1/v2 全部公共向量、Debug/Release、打包与严格验签通过；未执行真实网络或硬件动作 |
| 2026-08-28 | DS-007 设置、DDC 可靠性与 v2-only | 自动验证完成，GUI/实机待验 | 55fec44 | 47 项 XCTest、20 条 v2 消息向量、18 条 v2-only 状态机向量、Debug/Release、打包、严格验签和 v2 合同校验通过 |
| 2026-08-29 | DS-008 本机 USB 双向切换 | 自动验证完成，GUI/实机待验 | 本任务提交 | 49 项 XCTest、USB-001～016、20 条 v2 消息向量、6 条 v2 状态机向量、Debug/Release、打包、严格验签和两组合同校验通过 |
| 2026-08-29 | DS-008 macOS UDP 固定源端口 | 自动验证完成，双机实测待验 | 本任务提交 | 5 项传输资源测试、loopback 源/目标端口与回包验证、两组合同校验通过；完整 Xcode 验证见交接记录 |
| 2026-08-29 | DS-009 Apple Silicon 原生显示控制 | 止损回退完成，实机待验 | 本任务提交 | Native DDC 读写/service 管理恢复 `e2d0dd7`；92 项 XCTest、全新 Debug 测试构建、Release 打包和严格验签通过；四个自动入口零 DDC 读，只保留设置页显式读取 |
| 2026-08-29 | DS-009 输入源切换隔离 | 自动验证完成，实机待验 | 本任务提交 | 专用 VCP 0x60 服务每次重新解析短生命周 service，多显示器串行且零 GetVCP；6 项专项和 98 项全部 XCTest 通过 |
| 2026-08-29 | DS-009 输入 service 一次性租约 | 自动验证完成，30 次实机待验 | 本任务提交 | 只增加写后 1 秒、上限 32 的异步强引用租约；7 项专项和 99 项全部 XCTest 通过 |
| 2026-08-29 | DS-009 C2C 输入源链路诊断 | 自动验证完成，C2C/C2DP 日志待验 | 本任务提交 | 记录候选、选择、实际 WriteI2C 和同 service VCP 0x60 反馈；12 项专项、104 项全部 XCTest、构建和严格验签通过 |
| 2026-08-29 | DS-009 多显示器输入源并发 | 自动验证及用户实机验证通过 | 4756ef2 | 修复 USB 单目标包装串行点，USB/手动/协同统一批量入口；20 项相关测试、109 项全部 XCTest、Debug/Release、打包和严格验签通过；用户实机确认小米与 Dell 同时切换，不再先后串行 |
| 2026-08-29 | DS-009 macOS 协同检测诊断 | 自动验证完成，双机日志待验 | 本任务提交 | 保留 1 秒超时和协议 v2；24 项相关测试、115 项全部 XCTest、Release 打包和严格验签通过；未执行真实网络或硬件动作 |
| 2026-08-30 | DS-010 本地网络权限引导 | 自动验证完成，权限实机待验 | 本任务提交 | 11 项相关测试、118 项全部 XCTest、Debug/Release、打包和严格验签通过；普通失败不误报系统拒绝，未访问真实局域网或硬件 |
| 2026-08-30 | DS-011 原生 DDC 单后端清理 | 自动验证完成，实机待验 | 本任务提交 | 54 项相关测试、122 项全部 XCTest、Debug/Release、打包和严格验签通过；正式运行路径不含外部 DDC 进程或历史后端选择 |
| 2026-08-30 | DS-014 内建 HDMI 原生 DDC 读取诊断 | 正式安全策略完成 | 本任务提交 | 实机确认内建 HDMI 读取不可用后停止参数探测；50 项 DDC、135 项完整 XCTest、Debug/Release、打包和严格验签通过 |
| 2026-08-30 | M-008 / DS-015 IOAVService 拓扑绑定 | 自动验证完成，实机待验 | 039466b | 移除遍历顺序绑定并按当前 endpoint 一对一解析；54 项 DDC、139 项完整 XCTest、Release 打包和严格验签通过；PR #55 保持开放，未执行硬件动作 |
| 2026-08-30 | DS-017 内建 HDMI 生产读取事务 | 严格成功已确认，可靠性失败 | 533c507、180ba01 | 小米 HDMI 偶尔可在第 4/8 次严格成功，但随后连续 20 次失败；证明完整事务可读但不是可靠性根因 |
| 2026-08-30 | DS-018 IOAVService 生命周期稳定化 | 实机否定，代码已撤回 | 85691cf、后续 DS-019 提交 | 小米 HDMI 20/20 失败而 Dell C2DP 保持成功；证明 service 复用不是根因，不进入合并候选 |
| 2026-08-30 | DS-019 内建 HDMI Get VCP 请求校验和 | 完成 | 8ed8830、验收记录提交 | HDMI 20/20、C2DP 3/3 严格成功；56 项 DDC、141 项完整 XCTest、Release 打包、严格验签和 ZIP 校验通过 |
| 2026-08-30 | DS-020 扩展坞 HDMI Get VCP 校验策略 | 完成 | b2f22d7、验收记录提交 | 三条实机链路分别命中所需校验策略；扩展坞首次回退成功且后续缓存加速；60 项 DDC、145 项完整 XCTest、Release 打包、严格验签和 ZIP 校验通过 |
| 2026-08-30 | M-006 诊断与脱敏 | 完成 | 21dcbe3、9c09212、7e56bf6、验收记录提交 | 首版实机发现逐台 DDC 读取时全量重新枚举会清空前两台最后结果；修复后用户确认 D1/D2/D3 均保留各自成功状态且导出无禁止字段。PR #59 完成 149 项 XCTest、Release 打包、严格验签和 artifact 上传 |
| 2026-08-31 | DS-021 发布准备事实同步 | 文档完成；无运行时变更 | 本任务提交 | 完成 M-007 兼容性文档，记录 PR #59 合并与实机结果、C2DP 3/3 和 C2C 2/3 样本，以及源码/ad-hoc 测试包分发边界；TCC、通用 GUI 与未覆盖硬件场景继续保持待验 |
| 2026-08-31 | DS-022 稳定本地开发签名 | 自动验证完成，权限实机待验 | e0da129、验收记录提交 | 构建脚本可选使用本机有效 Apple Development 身份且默认保持 ad-hoc；本机两种签名模式与 149 项 XCTest 通过，PR #62 CI run 33376900709 完成 Debug、149 项 XCTest、Release、打包、严格验签及 artifact 上传；签名材料未入库，固定路径权限复用待用户验证 |
| 2026-08-31 | DS-023 按需详细诊断记录 | 自动验证完成，GUI 待验 | 本任务提交 | 默认关闭的全局开关门控 DDC、输入源和协同详细轨迹；切换清空会话记录，显示器页只保留单行结果；完整 XCTest 153/153 通过，真实 GUI 与硬件路径待用户验证 |
| 2026-08-31 | DS-024 托盘菜单收敛 | 组合自动验证完成，GUI 待验 | 本任务提交 | 隐藏无控件显示器分组，静态动作只保留设置/退出，联动读取持久配置且动态区域控制分隔线；与 DS-023 组合后定向 33 项、完整 159 项 XCTest、Release 构建、App 与 ZIP 解压副本严格验签通过，未执行真实硬件或网络动作 |
| 2026-09-02 | DS-025 USB 与协同设置布局对齐 | 自动验证完成，GUI 待验 | 6181b2a、10a3aa9、c08bcbb、3d42c4d、df7b02b、c0ce372、b98aa42、ecbce6c、本任务提交 | USB/协同各收敛为两组可滚动内容；修复布局与窗口保存反馈，并将 USB/协同对端输入源统一为可留空的部分映射；0/负数/越界值在存储、业务服务、DDC 路由和原生写入口均零写入。USB 与协同保存反馈按 scope 隔离；完整 XCTest 194/194、Release 构建、Apple Development 严格验签与 ZIP 完整性通过；真实 GUI/辅助功能待验 |
| 2026-09-02 | DS-027 联动 DDC 统一控件 | 自动验证完成，GUI/实机待验 | 6962ae9、审核修复提交 | 联动开启时设置页与托盘改为公共绝对值控件；值区分相同、混合、未知，不确定状态以无拇指中性轨道避免冒充值，上限取在线唯一目标安全交集，磁盘重载不污染运行时目标；定向 68/68、完整 XCTest 203/203、Release 构建及 App/ZIP 解压副本严格验签通过，真实 GUI/DDC 待验 |
| 2026-09-02 | DS-028 托盘与设置窗口生命周期 | 自动验证完成，GUI 待验 | 075e536、状态语义修复提交 | 托盘增加只反映持久化开关的匿名 USB 状态与全语义图标，DDC 行压缩为 252 点单行；学习/安全门只控制执行能力，不篡改设置状态文案；左右键只开菜单，设置窗口使用 regular/accessory 正常生命周期；定向 124/124 + 32/32、最新完整 XCTest 221/221 通过，真实 GUI 待验 |
| 2026-09-02 | DS-029 离线显示器安全手动删除 | 自动验证完成，GUI 待验 | fc064b1、可信度修复提交 | 检测保留离线配置；CG 身份与本机 IOAV 物理服务一一完整匹配后才累计两次缺失并允许确认删除，虚拟/漏枚举结果拒绝；原子级联清映射和稳定身份缓存，部分映射保留、最后映射安全停用且零硬件/网络动作；安全复核定向 95/95、最新完整 XCTest 221/221 通过，真实断开/重接待验 |
| 2026-09-03 | DS-030 macOS 托盘图标完整性与媒体键审计 | macOS 27 图标可见性自动验证完成，实机复核待验 | 5468c29、46f8a42、PR #77/#79 | 菜单可见语义统一使用 16 点 template SF Symbol 候选；状态栏改为 18 点、90% 主体、6% 线宽的“显示器 + 百分号”template image。后续用户实拍证明 macOS 27 会按默认 `.automatic` 隐藏标准菜单项 image，统一 `apply` 入口已显式设置 `.visible`，自定义行保持独立 `NSImageView`。DS007 定向 35/35、完整 XCTest 271/271、Release arm64、严格验签及 ZIP 完整性通过；标准菜单项实际渲染仍待新包复核。 |
| 2026-09-04 | DS-031 macOS 原生媒体键关联 DDC | F1/F2 与 VCP 0x62 实机通过，安全音量接管自动验证完成 | e251a2e、2c95e21、bfaa101、be15fd8、321dcdd、206ebf6、0e02ae3、5a32f07、46f8a42、PR #79 | 实机证明 F10/F11/F12 的 DDC 音量写已成功，残留系统 OSD 根因是 listen-only tap。新增默认关闭的 HDMI/DP opt-in：非显示/系统可调路由纯系统且零 DDC；符合条件路由首次全目标 fresh read/write 成功后保持 session armed，不因普通闲置超时退出，但每次已吞动作仍需自己的新鲜读取且不可复用旧值。输出/拓扑/配置/权限/tap/读写失败均 fail-open，F1/F2 永不吞。常规页权限行已去除固定高度空容器；HUD 使用公开单层玻璃材质；macOS 27 标准菜单项显式请求 image 可见。定向 47/47 + 35/35、完整 XCTest 271/271、Release arm64、严格验签及 ZIP 完整性通过；实机接管、HUD 与图标渲染仍待验。 |

### DS-007 / DS-031 审核修复：离线协同与 DDC 代次隔离

- [x] 离线目标的手动协同发送一次请求后取消本次事件，提示未切换；迟到确认和计时器不能执行 DDC。最近在线目标保留 600 ms 兜底。
- [x] 已取消代次的 DDC 回调不再删除新代次的活动标记，连续写入继续合并为在途值和最新待写值。
- [x] 删除生产代码未消费的 HUD 固定行为模型及其自证式测试，实际窗口设置保持原样。
- [x] 272 项 XCTest、Release 构建及严格签名验证通过；新增离线/迟到确认/恢复在线和取消后重启写入回归。
- [ ] 真实离线协同交互、连续媒体键与显示器重新检测交错仍需实机验证。
