# DS-002 / W-004：Windows GitHub Actions CI

## 基本信息

- 功能编号：DS-002
- 平台清单：W-004 Windows CI
- 任务类型：windows-only
- 当前状态：COMPLETE
- 协调基线：`d9f139096fc8f304836cfa8169653d1a8ab225bf`
- 协调分支：`codex/coord-ds-002`
- 公共协议：`PROTOCOL.md`，保持 `version = 1`
- 公共数据：保持 contracts `schemaVersion = 1`，不新增或修改公共数据结构
- proposal：不需要；本任务不改变协议或公共行为
- contract：继续消费 `contracts/protocol-v1/` 的 17 条消息向量和 16 条状态机向量，不修改 contracts

## 公共行为定义

本任务只把现有 Windows 构建、自动测试和绿色版产物检查搬到 GitHub 托管的 Windows runner。不得改变应用运行时、协议字段、配置语义、硬件行为或 macOS 实现。

CI 必须在全新 runner 和空项目缓存条件下完成：

1. 检出仓库并使用只读仓库权限。
2. 使用支持 Visual Studio 18 和 `v145` 的 GitHub 托管 Windows x64 runner。
3. 恢复 C++/WinUI 3 项目依赖。
4. 构建 Windows Native、Launcher 和 Tests 的 x64 Release。
5. 执行无硬件自动测试，并明确通过 17 条消息向量和 16 条状态机向量。
6. 运行现有 `Windows/build-windows.ps1` 生成 `Windows/dist/`。
7. 验证根入口、`runtime/` 必需文件和小于 20 MiB 的现有体积限制。
8. 上传framework-dependent 的 Windows x64 测试 artifact；不得提交构建产物。

## Runner 与工具链决定

三个正式 Windows 工程当前都声明 `MinimumVisualStudioVersion = 18.0` 或 `PlatformToolset = v145`。因此 workflow 应显式使用 GitHub 官方提供的 `windows-2025-vs2026`，不要依赖会迁移的 `windows-latest`，也不要降级工程工具集来迎合旧 runner。

该 runner 选择只属于 CI 环境，不改变应用最低 Windows 版本或发布依赖。若官方 runner 暂时缺少项目需要的 Visual Studio 组件，平台 Agent 应先记录准确错误并最小化补齐 CI 环境；不得擅自改用旧工具集或修改运行时代码。

## Windows 任务范围

- 分支：`codex/windows-w004-ci`
- 允许修改：
  - `.github/workflows/windows.yml`
  - `Windows/DEVELOPMENT_CHECKLIST.md`
  - `handoffs/windows.md`
  - 仅当 CI 暴露可复现的构建脚本缺陷时，允许最小修改 `Windows/build-windows.ps1`
- 禁止修改：
  - `macOS/`
  - `handoffs/macos.md`
  - `PROTOCOL.md`
  - `AGENTS.md`
  - 根目录 `README.md`
  - `specs/`、`contracts/`、`coordination/`
  - Windows 应用运行时代码和项目工具集，除非另行报告并获得协调批准

## Windows 分支、提交和 PR

- 分支：`codex/windows-w004-ci`
- 最终分支提交：`2277fdbbd79f305695c437834cf21c1e029ff244`
- 功能提交：`4ffd077c44d911c72a5a9db40ea79317b952c4df`
- PR：[#9](https://github.com/maizihk/DisplaySwitch/pull/9)，已合并为 `71df0e7c018aba18e33bf3363173e1df255323e3`
- PR CI：Windows run `33056123299` 在功能提交上首次成功；最新分支 run `33056794721` 也成功
- 合并后 CI：`main@71df0e7` 的 Windows run `33057579020` 与 macOS run `33057579043` 均成功

## macOS 范围与基线

- macOS 不需要修改。
- 合并后 `main@71df0e7` 的 macOS workflow run `33057579043` 已通过。
- Windows CI PR 必须继续让现有 macOS workflow 保持通过，但不得为 W-004 修改 macOS workflow 或源码。

## 自动测试与公共向量

- CI 必须实际运行 `DisplaySwitcher.Tests.exe`，不能只完成编译。
- 测试日志必须显示通过 17 条消息向量和 16 条状态机向量。
- 测试不得访问真实网络、USB、DDC、显示器唤醒或输入源切换。
- workflow 不得使用真实配对码、IP、设备标识、本机路径、证书或签名密钥。

## Windows CI 验收结果

- runner：`windows-2025-vs2026`，实际使用 Visual Studio 18.9 / MSBuild 18.9.1。
- 全新托管 runner 完成三个原生 C++ 工程的依赖恢复和 x64 Release 构建。
- 构建脚本及显式测试步骤均输出通过 17 条消息向量和 16 条状态机向量。
- `Windows/dist/` 包含根入口及 6 个 `runtime/` 必需文件，总计 1.30 MiB，低于 20 MiB 限制。
- 合并后 artifact `DisplaySwitcher-Windows-x64-unsigned-framework-dependent` 上传成功，artifact ID `9640415507`，压缩大小 637,900 bytes，保留 7 天。
- workflow 仅有 `contents: read`，未使用 secrets、签名材料、真实配对码、IP、硬件标识或个人路径。

## 未解决差异和阻塞

- 当前无协议、平台行为或 CI 阻塞。
- Windows workflow 使用路径过滤；Windows、contracts、`PROTOCOL.md` 或 workflow 自身变化会触发 Windows CI。

## 需要用户决定的问题

- 无。用户已明确授权开始 W-004，并允许本任务新增 Windows GitHub Actions workflow。
- 本任务不授权购买 runner、签名证书或其他付费服务；优先使用仓库可用的 GitHub 托管标准 runner。

## 仍需实机验证

无。W-004 是构建基础设施任务，不需要真实 DDC、USB、睡眠/唤醒、防火墙或双机测试。

## 回滚方式

- 回退 W-004 PR 或删除新增的 Windows workflow，即可恢复到仅有 macOS CI 的状态。
- workflow artifact 是临时 CI 产物；回滚不修改用户配置、协议或平台运行时代码。

## 合并建议

当前结论：READY_TO_MERGE（W-004 PR 已按该结论完成合并）。

原因：GitHub 托管 Windows runner 已在 PR 和合并后 main 上完成依赖恢复、x64 Release、17+16 向量、dist 结构/体积检查和 artifact 上传；macOS 主分支 CI 同时保持通过。W-004 不涉及真实硬件验证。
