# DS-006 公开仓库安全与文档整备

- 功能编号和名称：DS-006 公开仓库安全与文档整备
- 任务类型：coordination-only
- 当前状态：COMPLETE
- 起始基线：`53c2197a75ee46a50ee1bdf31388345325ead0f5`
- 协调分支：`codex/coord-ds-006-public-readiness`

## 公共行为定义

本任务不改变应用运行行为、网络消息或本机配置语义。目标是让公开仓库的默认治理、公开下载入口、隐私说明和项目文档与当前实现保持一致。

公开仓库应满足：

- `main` 不允许删除或 force push，变更必须通过 PR。
- 自动化令牌保持最小只读权限；平台 CI 继续使用路径过滤，避免无关文档触发平台构建。
- Secret Scanning、Push Protection、Dependabot alerts/security updates 和私下漏洞报告可用。
- 旧的、不代表当前安全基线的安装包不作为公开正式下载入口。
- README 不暗示当前存在已签名、已公证或已经完成实机验收的公开发行版。
- 当前树、公开样例和后续提交不得包含真实 IP、配对码、个人路径、USB/显示器标识或个人硬件配置。

## 协议与公共数据状态

- 网络协议：`version = 1`，不变。
- 本机配置：`schemaVersion = 3`，不变。
- proposal：无；本任务不提出协议变更。
- contract：无新增或修改。
- DS-005 的协议 v2 proposal/contracts 状态不受本任务影响。

## GitHub 设置核验

- 仓库可见性：PUBLIC。
- 默认分支：`main`。
- MIT License：已被 GitHub 识别。
- ruleset：`Protect main`，active，目标为 `refs/heads/main`。
- ruleset 规则：禁止删除、禁止 non-fast-forward、要求 PR、要求解决 review conversation；审批数为 0，适配当前单维护者流程。
- Actions 默认工作流权限：read；不得批准 PR。
- Secret Scanning：enabled。
- Push Protection：enabled。
- Dependabot alerts：enabled，审计时无开放告警。
- Dependabot security updates：enabled。
- Private Vulnerability Reporting：enabled。

## Release 状态

- 历史 Release：`v2.1.0`，tag 指向 `6af7f847be3e30eb151c5f5b6b33886d4f9c2937`。
- 审计发现该 Release 早于 W-001 安全默认值清理，旧安装包不代表当前 `main`。
- 已将 Release 临时改为 Draft；tag 和 macOS/Windows 两个资产均保留，没有删除或重写历史。
- 恢复公开前必须另行获得用户批准，并完成版本、源码基线、构建、校验和及实机限制核对。

## 当前树与历史审计

- 当前正式平台代码未发现私钥、GitHub token、云密钥、真实配对码、提交的构建产物或个人用户目录。
- Git 历史保留早期个人硬件默认值、设备路径和私网地址字面量；未发现可用凭据。
- 不建议为非凭据内容 force push 重写历史：公开后的副本无法撤回，且会破坏提交、PR 和 handoff 引用。
- `Windows/DEVELOPMENT_CHECKLIST.md` 当前仍引用一个旧 USB VID/PID；该文件由 Windows Agent 清理，协调 Agent 不得代改。
- 若任何历史字符串曾被用作真实配对码或凭据，应立即在对应系统轮换；当前审计没有发现此类硬编码值。

## Windows 任务范围

完成记录：分支 `codex/windows-ds-006-public-docs`，文档提交 `69c867c281421e9c929283ac28372296075a46fa`，最终 head `0ccbb8ad08467e6f03edec2dce9798d1cec73841`，PR [#27](https://github.com/maizihk/DisplaySwitch/pull/27)，合并提交 `35fefa06523cc1d44c5bdc5e1737ce81380eba4f`。PR head Windows run `33100156315` 与合并后 main run `33101301744` 均成功。

- 清理 `Windows/DEVELOPMENT_CHECKLIST.md` 中的具体旧 USB VID/PID，改成不包含真实标识的描述。
- 更新 `Windows/README.md`，使用“协同配置”和自定义名称，不再把唯一对端固定称为 Mac。
- 准确描述 W-201 的每显示器后端、亮度/对比度/音量、显式读取/提交、全零保护和未实机验证边界。
- 不修改协议、contracts、macOS、根 README、GitHub Actions、Release、tag 或版本号。

Windows Agent 可复制提示词：

```text
功能编号：DS-006 / Windows 公开文档清理
当前基线：以合并 DS-006 协调 PR 后的 main 为准
分支：codex/windows-ds-006-public-docs
允许修改：Windows/、handoffs/windows.md
禁止修改：macOS/、handoffs/macos.md、PROTOCOL.md、AGENTS.md、根 README、coordination/、specs/、contracts/、GitHub Actions、Release、tag、版本号

完整阅读 AGENTS.md、README.md、PROTOCOL.md、coordination/DS-006.md、Windows/DEVELOPMENT_CHECKLIST.md 和 handoffs/windows.md。
清理 Windows 开发清单中的具体旧 USB VID/PID；更新 Windows/README.md，使其与 schema v3 多协同配置、角色中性名称和 W-201 DDC 控制现状一致。不得删除旧 C# 迁移读取所需的兼容字段；不得写入真实 IP、配对码、设备路径或个人硬件信息。
只做文档和隐私清理，不执行真实 DDC、USB、网络或系统设置操作。运行文档链接/敏感信息检查及与风险相称的现有自动测试；更新 handoffs/windows.md，正常 commit、fetch、push 并创建 PR，不得 force push、合并、创建 Release/tag 或修改版本号。
```

## macOS 任务范围

完成记录：分支 `codex/macos-ds-006-public-docs`，实现提交 `55d933bd077d4f04fab5885e38fed70df1393ad1`，最终 head `3097d21f6b3d0ad4d7a180857e9ab803c18462d8`，PR [#26](https://github.com/maizihk/DisplaySwitch/pull/26)，合并提交 `30b6d6966c38c155d30db6435c6b4b29e55f1c31`。PR head macOS run `33099492324` 与合并后 main run `33101306471` 均成功。

- 核对现有关于页面是否满足 DS-004 C-023。
- 将公开测试和平台文档中的设备品牌标签改为中性模拟名称，但保留“三项全零不可信”的测试语义。
- 不执行真实显示器回读，不修改版本号、Release 或签名设置。

macOS Agent 可复制提示词：

```text
功能编号：DS-006 / macOS 公开文档与样例清理
当前基线：以合并 DS-006 协调 PR 后的 main 为准
分支：codex/macos-ds-006-public-docs
允许修改：macOS/、handoffs/macos.md
禁止修改：Windows/、handoffs/windows.md、PROTOCOL.md、AGENTS.md、根 README、coordination/、specs/、contracts/、GitHub Actions、Release、tag、版本号

完整阅读 AGENTS.md、README.md、PROTOCOL.md、coordination/DS-006.md、macOS/DEVELOPMENT_CHECKLIST.md 和 handoffs/macos.md。
核对关于页面 C-023；把公开测试/文档里的具体设备品牌标签替换为中性模拟显示器名称，同时保持 C-024 三项全零不可信、缓存不覆盖和真实原生读数未验证的语义。不得执行真实 DDC、USB、网络、唤醒或签名信任修改。
运行相关 XCTest、Debug/Release 构建、build-app 和严格 codesign 验证；更新 handoffs/macos.md，正常 commit、fetch、push 并创建 PR，不得 force push、合并、创建 Release/tag 或修改版本号。
```

## 公共验收

- GitHub 安全设置与 API 回读一致。
- 根 README 与当前 `main` 行为一致，不宣传 Draft Release 为正式版本。
- 当前差异不包含平台代码、协议、schema、workflow、版本或构建产物。
- `contracts/protocol-v1/validate.py` 和 `contracts/protocol-v2/validate.py` 继续通过。
- 后续两个平台文档 PR 独立验收，不互相修改共享文件。

## 未解决差异与用户决定

- 是否恢复或替换 v2.1.0 Release：需要用户在完成新发布验收后明确批准。
- 是否强制 Actions 使用提交 SHA：当前 workflow 使用 GitHub 官方 action 的大版本标签；若要强制 SHA，需用户另行授权修改 GitHub Actions。
- 是否重写公开 Git 历史：不建议；只有发现真实有效凭据时才重新评估，并必须取得用户明确授权。

## 实机验证

本任务不需要也不授权真实 DDC、USB、显示器唤醒、输入源切换、网络或防火墙验证。

## 回滚方式

- GitHub ruleset、安全功能和 Release Draft 状态可在仓库设置中分别撤销。
- README 与本协调记录通过普通 revert 回滚。
- 不删除 tag、Release 资产或 Git 历史。

## 合并建议

当前为 COMPLETE。协调 PR #25、macOS PR #26 和 Windows PR #27 均已合并；GitHub 设置已通过 API 回读，根与平台 README、公开样例和交接记录已按当前能力修订，协议 v1/v2 验证器均通过。真实硬件验证和正式 Release 属于后续独立阶段，不阻塞 DS-006 完成。
