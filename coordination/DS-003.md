# DS-003 跨平台根 README 重构

- 功能编号和名称：DS-003 跨平台根 README 重构
- 任务类型：coordination-only
- 当前状态：COMPLETE
- 当前基线：`956243777a461d03fe8cf6514a486a96378b18af`

## 公共行为定义

根 README 应作为 DisplaySwitch 的跨平台入口，准确区分 macOS 菜单栏实现、Windows 托盘实现、双端协同、构建产物、自动测试与尚需实机验证的限制。文档不得让新用户误以为项目只支持 macOS，也不得把 CI 测试包描述为正式发行版。

本任务只调整共享文档，不改变应用、网络、USB、DDC、配置迁移或用户运行时行为。

## 协议和公共数据

- 协议 version：`1`，不变。
- 数据 schemaVersion：`1`，不变。
- 相关 proposal：无。
- 相关 contract：`contracts/protocol-v1/`，仅引用，不修改。

## Windows 范围与验证

- Windows 任务范围：无平台代码任务；README 仅概述正式 C++/WinUI 3 实现、构建入口、Windows App Runtime 2.4 x64 依赖、DDC 后端和 CI。
- Windows 分支、提交和 PR：不适用。
- Windows 构建、测试和 CI：复用 `main@71df0e7c018aba18e33bf3363173e1df255323e3` 的成功 run `33057579020`；DS-003 不修改 Windows、contracts、PROTOCOL 或 Windows workflow，因此路径过滤不会触发 Windows CI。

## macOS 范围与验证

- macOS 任务范围：无平台代码任务；README 仅概述正式 Swift/AppKit 实现、构建入口、产物、DDC 后端和 CI。
- macOS 分支、提交和 PR：不适用。
- macOS 构建、测试和 CI：PR #11 最终 head run `33059257186` 通过，用时 1 分 4 秒；合并后的 `main@578f267cad867c9a376353a15c4538c44666923a` run `33059368104` 通过，用时 1 分 5 秒。两次均完成 Debug、XCTest、Release、打包、严格验签和测试 artifact 上传。

## 公共测试向量

README 记录两端当前共同通过 17 条消息向量和 16 条状态机向量。DS-003 不修改向量或实现，不重新执行真实硬件测试。

## 差异、决定与实机验证

- 已解决差异：原 README 的标题和主体偏向 macOS，Windows 与公共协议信息埋在后部；新结构将跨平台用途、两端快速开始、协同、安全与 CI 分开呈现。
- 需要用户决定的问题：无。
- 仍需实机验证：README 中描述的真实 USB 交接、DDC 输入源切换、显示器唤醒以及不同硬件链路兼容性仍以平台 handoff 为准；本任务未执行这些操作。

## 安全与隐私检查

- 不记录真实显示器 UUID、USB 标识、IP、配对码或本机路径。
- 删除 README 中带具体显示器序号和输入源值的 `m1ddc` 示例，避免将特定硬件配置误当作通用默认值。
- 明确 UDP v1 配对码不是加密或强身份认证，并要求仅在可信局域网使用。
- 明确 CI artifact 是测试产物，不是 Release。

## 回滚方式

回退 DS-003 的 README 和协调记录提交即可。协议、contract、平台实现和配置数据均不受影响。

## 合并建议

`READY_TO_MERGE`，且已执行：PR #11 已于 2026-08-27 合并为 `578f267cad867c9a376353a15c4538c44666923a`。Markdown/本地链接目标审计、敏感信息扫描、公共向量结构校验、PR CI 和合并后 main CI 均通过；变更仅涉及 README 与本协调记录。
