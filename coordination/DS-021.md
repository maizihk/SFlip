# DS-021 release-readiness truth sync

- Task: DS-021 release-readiness truth sync
- Type: coordination-only documentation
- Baseline: `main@ee6bc5bacc582841351c4b89b23ae842151a21cc`
- Pull request: [#61](https://github.com/maizihk/DisplaySwitch/pull/61)
- Runtime behavior: unchanged
- Protocol: remains `version = 2`
- Local configuration: remains `schemaVersion = 5`
- Contracts and workflows: unchanged
- Release policy: source and build packages; Developer ID, notarization, and commercial signing remain deferred. The maintainer has since authorized macOS DMG and Windows installers, with neutral package names

## Root cause

The platform implementations and hardware acceptance advanced faster than the public documentation. After PRs #54, #59, and #60 merged, several durable records still described stacked PRs as open and CI as absent. Public files also described removed m1ddc and ControlMyMonitor fallbacks, protocol v1, the retired multi-target `input_present` flow, and an obsolete USB/network ordering.

Leaving those statements in place would cause users and future contributors to configure features that no longer exist or repeat completed investigations.

## Scope

1. Add one public compatibility document covering private macOS API risk, native-only platform paths, connection-path differences, validation levels, safe test order, and an anonymized report template.
2. Align root README, support, security, and Windows instructions with current v2-only, schema v5, native-only behavior.
3. Record the final merge and CI state of Windows PRs #54/#60 and macOS PR #59.
4. Close only the real-device acceptance items explicitly confirmed by the user; keep sleep/resume, hot-plug, interface-change, TCC allow/deny, DPI/theme, and untested VCP combinations open where evidence is incomplete.
5. Record that the macOS local-network permission module is already implemented. Only real TCC allow, deny, and re-allow validation remains.

## Evidence recorded

- PR #54 merged as `e14ae6ea6d381dd31097406d7d735f41ec9a2699`; Windows CI run `33366897393` passed build, tests, distribution verification, and artifact upload.
- PR #60 merged as `3a22c66afdb4838040e2fdc5d122ed955337bb13`; Windows CI run `33367712427` passed the same main-baseline pipeline, and the user accepted the Windows test build.
- PR #59 merged as `ee6bc5bacc582841351c4b89b23ae842151a21cc`; macOS CI run `33316481986` passed, and the recorded D1/D2/D3 diagnostic acceptance remained intact.
- The observed macOS C2DP diagnostic sample succeeded 3/3, while the corresponding C2C sample succeeded 2/3. This closes the requested sample collection but documents C2C as less reliable on that tested path rather than inventing a universal connector rule.

## Explicit non-goals

- No application, protocol, contract, schema, workflow, version, tag, Release, system permission, firewall, DDC, USB, wake, or input-source change.
- No Bonjour/mDNS discovery and no first-run wizard.

## Validation

- Relative Markdown link check passed across repository documentation.
- `contracts/protocol-v2/validate.py` passed: 4 schemas, 1 normalization vector, 4 authentication vectors, 20 message vectors, and 6 state-machine vectors.
- `contracts/usb-switch-v1/validate.py` passed all 16 schema-v5 USB switch vectors.
- Public-guidance scan found no current claim that protocol v1 or a removed external DDC fallback remains active; historical checklist entries are explicitly labelled historical.
- Final diff review is limited to Markdown documentation and contains no runtime, project, contract, workflow, version, generated output, secret, or raw hardware identifier.

## Merge recommendation

`READY_FOR_REVIEW`. This documentation task does not require hardware actions or platform rebuilds because it changes no executable, project, resource, protocol, contract, workflow, or version file.
