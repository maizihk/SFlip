# DS-008 本机 USB 双向切换

- 功能编号和名称：DS-008 本机 USB 双向切换
- 任务类型：cross-platform
- 当前状态：CROSS_PLATFORM_VALIDATION
- 当前基线：`main@a44e3d1ddf5fa8feadc968b3ae392eb7fb13ae2b`
- 公共行为：USB 离开立即执行本机 DDC；USB 接入只唤醒本机显示器；可选联动只并行发送网络唤醒且不影响切换
- 协议 version：保持 `version = 2`；用户已批准删除 USB 网络协同并增加可选 `wake_display`
- 数据 schemaVersion：已从 4 升到 5，保留非 USB v4 数据并重建关闭的独立 USB 配置
- proposal：`specs/proposals/DS-008-local-usb-switch.md`
- contracts：`contracts/protocol-v2/`、`contracts/usb-switch-v1/`

## 公共行为定义

1. 两端 USB 功能只监听用户明确选择的一个本机 USB 设备。
2. 初始状态只建立基线，零硬件和网络副作用。
3. `接入 → 离开` 不等待网络、心跳、确认或应用层防抖，立即异步执行本机显示器输入源切换。
4. `离开 → 接入` 只请求一次本机显示器唤醒，不执行 DDC。
5. 重复状态通知幂等；学习、安全状态、配置重载和关闭期间零副作用。
6. USB 配置独立保存设备和每显示器目标输入源；联动关闭时不读取协同配置。
7. 联动开启时只并行发送一次认证 `wake_display`；发送失败不影响 DDC，目标端与本机 USB 接入唤醒在 2 秒内合并为一次。

## Windows

- 任务范围：独立 USB 状态转换、v5 本机配置、USB 页、模拟测试和本地 Release 构建。
- 分支：`codex/windows-ds-008-usb-local-switch`
- 实现提交：`43683d6973e2088ba3e4832a622102cffd081633`
- PR：[#42](https://github.com/maizihk/DisplaySwitch/pull/42)，已合入协调分支。
- 本地验证：x64 Release 构建和原生测试通过；消费 USB-001～USB-016、20 条协议消息和 6 条协议状态机向量。
- CI：[Windows run 33218056242](https://github.com/maizihk/DisplaySwitch/actions/runs/33218056242) 通过，包含 x64 Release、原生测试、绿色版打包和产物检查。

## macOS

- 任务范围：USBMonitor 与 v2 路由解耦、独立 USB 状态转换、v5 本机配置、USB 页、模拟测试、Debug/Release/打包/验签。
- 实现分支：`codex/macos-ds-008-usb-local-switch`，实现提交 `559d285aa194e5e7a0cf0ccd9bff90bd8abbafea`，PR [#41](https://github.com/maizihk/DisplaySwitch/pull/41) 已合入协调分支。
- 端口修复分支：`codex/macos-ds-008-fixed-source-port`，实现提交 `bc37d2e214eff1cd514d82878c78e48b8d939966`，PR [#43](https://github.com/maizihk/DisplaySwitch/pull/43) 已合入协调分支。
- 本地验证：DS-008 实现提交完成 49 项 XCTest、Debug/Release、打包和严格验签；端口修复完成 Swift 类型检查、5 项可注入传输测试代码审查、loopback 源端口/回包实测及两组合同校验。
- CI：[macOS run 33218056211](https://github.com/maizihk/DisplaySwitch/actions/runs/33218056211) 通过，包含端口修复后的完整 XCTest、Debug/Release、打包和严格验签。

## 整合与预览发布

- 最终 PR：[#44](https://github.com/maizihk/DisplaySwitch/pull/44)，双端 CI 通过后合入 `main`。
- 合并提交：`a44e3d1ddf5fa8feadc968b3ae392eb7fb13ae2b`
- 预览标签：`ds008-preview-2026-08-29`
- macOS ARM64 临时签名包 SHA-256：`57ba69363463c455b56c2090a1e22023a11bcc94a2fcb3c25000a4469d26d2ba`
- Windows x64 绿色版 SHA-256：`e36873b6e318253e248c95e4e1fed08a7f8a07f6056e5d400831f7db7a6a0348`
- 预览包只用于用户实机验证，不代表正式稳定 Release。

## 公共测试向量

`contracts/usb-switch-v1/` 定义 USB-001 至 USB-016。它们只使用逻辑 presence、模拟 DDC、模拟唤醒和模拟网络，不记录真实 USB 或显示器标识。

## 已解决差异

- 两端 USB presence 已从 v2 协同状态机移除，改为独立本机协调器。
- macOS USB 监听已与 v2 路由解耦；Windows 显示器目标已改为独立 USB 映射。
- 公共协议已删除旧 USB 网络交接，联动只允许认证 `wake_display`。
- macOS 主动 UDP 已从临时源端口改为复用本机 `listenPort`；目标端口仍使用配置的 `peerPort`，与 Windows 首次来源校验一致。

## 阻塞问题及责任

- 协调：无自动验证阻塞；等待用户使用预览包完成实机验证。
- 平台：无已知自动验证阻塞。

## 仍需实机验证

- 双向真实 USB 交接、真实 DDC 输入源切换和本机显示器唤醒。
- 程序重启、USB 瞬断、重复通知和显示器部分失败。
- macOS/Windows 首次探测、心跳、手动协同和 `wake_display` 双机实测。

未经用户在对应任务中再次确认，不执行上述硬件动作。

## 回滚

回退最终整合 PR，或分别回退 #43、#42、#41 和共享规范提交。保留 v4 本机备份；回滚后 USB 自动切换保持关闭。

## 合并建议

`NOT_READY`：双端实现、最终 CI 和预览包校验通过；真实 USB、DDC、唤醒及跨端网络行为尚未实测，因此不建议标记为正式稳定版。
