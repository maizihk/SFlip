# 跨平台提案

`PROTOCOL.md` 是当前生效的双端通信规范。平台开发任务不得直接改变它。

需要修改共享协议、公共数据格式或跨端行为时，先在本目录提交独立提案。提案至少应包含：

- 背景、目标和不处理的范围。
- 协议版本或 `schemaVersion` 变化。
- Mac 与 Windows 的兼容和拒绝策略。
- 旧数据迁移与失败时的安全行为。
- 不含真实设备标识、IP、配对码或本机路径的测试样例。
- 双端实施顺序、回滚方法和验收条件。

提案通过评审后，才能修改 `PROTOCOL.md`、公共测试向量和两端实现。

## 已批准提案

- [DS-026：输入源空值与零值安全约束](DS-026-input-source-null-safety.md)（2026-09-02）：输入源映射使用 `null` 或 `1...65535`，双端缺失映射统一报告 `missing_mapping`。

- [DS-039：配对码直接连接](DS-039-pairing-code-connections.md)（2026-09-11）：取消人工身份绑定，状态交换自动更新路由缓存。
