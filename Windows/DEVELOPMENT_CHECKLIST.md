# Windows 端开发清单

> Windows 端开发遵守仓库根目录 `AGENTS.md` 的通用开始、结束、Git 协作和硬件安全规则。
> 本文件只记录 Windows 任务、验收条件和进度，不重复通用约束。

## 当前边界

- 正式 Windows 实现是 `Windows/DisplaySwitcher.Native/`，技术栈为原生 C++/WinUI 3。
- `Windows/DisplaySwitcher.Launcher/` 是绿色版入口启动器，正式构建仍需要它。
- `Windows/DisplaySwitcher.Windows/` 是旧 C# 迁移参照，不参与正式构建；不要在其中实现新功能。
- `PROTOCOL.md` 是 Mac/Windows 通信的唯一规范；当前正式运行时为 v2-only。未经双端同步设计，不得单方面改变消息结构或行为。
- macOS 与 Windows 均已完成动态多显示器模型；Windows 端当前仅剩设置页和高 DPI 交互的实机验证待确认。
- 不要重写现有托盘、WinUI 3、USB 监控、UDP、DDC 或登录启动实现，只做可验证的渐进式迁移。

## P0：公开源码前必须完成

### W-001 清除个人硬件默认值

- [x] 新安装不再预设任何具体 USB VID/PID、外部工具路径、显示器路径、型号或输入源编号；DS-011 已删除正式配置中的外部 DDC 工具字段。
- [x] 新安装保持“未配置且不会执行 USB/DDC 动作”的安全状态。
- [x] W-001 当时保留已保存配置；DS-007 的不兼容升级现改为先备份 v3 或更早文件，再生成安全关闭的 v4 配置。
- [x] UI 使用通用名称，不再写死“小米”“Dell”或固定的两台显示器。
- [x] 清理 `README.md`、`Windows/README.md` 中作为产品默认值出现的个人硬件配置；如保留示例，必须明确标记为示例并使用中性占位值。
- [x] 配对码、本机路径、IP、设备 ID 和用户目录不得写入源码、测试快照或日志。

验收：删除本机配置后启动，不会切屏或触发 USB；设置页明确提示用户完成配置。

### W-002 动态多显示器模型

- [x] 用显示器配置集合替代 `redmi*` / `dell*` 固定字段，支持 0、1、2、3 台及更多显示器。
- [x] 每台显示器至少保存稳定标识、用户名称、原生 DDC 标识和各协同配置对应的对端输入源；不再保存兼容后端设备路径。
- [x] 使用稳定标识匹配显示器，不能依赖枚举顺序或 `DISPLAY1` / `DISPLAY2` 永久不变。
- [ ] 设置页可以添加、删除、重排和重新选择显示器；布局适配高 DPI。（代码完成；高 DPI 与实机交互待确认）
- [x] 待确认显示器提供显式重新绑定入口；只接受当前可信拓扑中强身份、逻辑目标和单物理句柄均唯一且未被其他目录占用的候选，确认时重新枚举，保存失败保留旧目录与映射。
- [x] 动态显示器配置只按稳定原生 DDC 标识关联；DS-011 已从正式实现删除外部工具后端和选择逻辑。
- [x] W-002 曾提供旧双显示器迁移；DS-007 后不再执行该迁移，而是保留旧文件备份并安全重建 v4。
- [x] 未配置输入源或找不到显示器时只报告问题，不猜测输入值、不误操作其他显示器。

验收：用虚拟/模拟配置覆盖 0、1、2、3 台显示器、枚举顺序变化和显示器替换场景。

### W-003 交接状态机可测试化

- [x] 从 `Controller` 中分离纯状态机；网络、USB、时钟、唤醒和 DDC 由 Controller 适配器输入或执行状态机动作，可在测试中替换。
- [x] 手动协同继续使用虚拟时钟测试 150 ms 重发、最多 4 次、600 ms 兜底和 6 秒在线窗口；DS-008 已从网络状态机删除 USB 防抖和发现路径。
- [x] DS-008 后 USB 状态由独立本机协调器处理，不再输入 V2 状态机；手动请求、确认丢包、对端离线和 `committed` 收尾保持覆盖。
- [x] 覆盖重复、过期、乱序、错误 source/target、错误配对码和错误 version。
- [x] `status_probe` / `status_response` 使用相同 `eventID`，且状态探测绝不触发唤醒、USB 或 DDC。
- [x] 历史 v1 状态机曾消费 17 组消息和 16 组状态机向量；DS-007 后正式工程已删除该状态机和这些测试引用。

验收：测试不依赖局域网和真实硬件，可重复运行且结果稳定。

### W-004 Windows CI

- [x] 新增 GitHub Actions Windows 工作流，至少执行依赖恢复、x64 Release 构建和自动测试。
- [x] 验证 `Windows/build-windows.ps1` 生成完整 `Windows/dist/`。
- [x] 检查入口、runtime 文件和体积限制。
- [x] CI 不包含签名私钥、配对码、个人路径或硬件 ID。
- [x] 构建产物可作为 workflow artifact 下载，但不能提交到 Git。

验收：全新 GitHub Actions runner 可以从空缓存完成构建和测试。

### DS-004 第一阶段：本机协同模型与 UI（历史实现，配置格式已由 DS-007 取代）

- [x] 本机配置升级为 `schemaVersion = 3`，随机生成并持久保存 `localEndpointID`，显示器目录与协同配置分离。
- [x] 协同页支持添加、删除、重命名、排序和多个配置同时开启；删除已开启配置前要求确认，且至少保留一个配置。
- [x] 每个配置保存对端主机、端口、NFC 配对码、已确认 endpoint/协议能力、显示器输入映射和本机触发设备引用。
- [x] Windows v2 配置迁移为一个旧对端配置，`MacInput` 迁入对端映射，`localInput` 保持 `null`，原文件保留备份。
- [x] 迁移、解析，以及正常 v3 设置保存的编码、临时写入、完整回读或原子替换失败时保留原数据并持久进入安全状态；当前实例立即停止协同和硬件副作用，只有用户成功保存合法 v3 配置才解除。
- [x] 托盘菜单按已启用且本机完整的配置名称生成，不增加“切换到本机”，多配置不退化选择第一项。
- [x] “检测”仅执行本机字段、显示器引用和缓存后端可用性检查，不发送网络消息或调用硬件动作。
- [x] 临时配置自动测试覆盖 C-001 至 C-015、边界校验、迁移、安全重启和显示器失败隔离；继续通过 v1 17+16 公共向量。
- [x] USB 学习按稳定目标锁定会话并在 30 秒内收集候选；DS-009 进一步支持基线设备离开后重新接入。多候选必须明确选择，取消、超时、配置删除和迟到代际均不改原绑定。
- [x] USB 学习及候选确认期间暂停 UDP、自动 USB 交接、DDC、唤醒和旧异步结果；纯模拟覆盖 C-021、C-022。
- [x] 常规页提供关于页面，只使用公开产品、版本、架构、协议和项目链接数据；不读取本机配置，纯自动覆盖 C-023 零副作用和隐私边界。
- [ ] WinUI 3 配置增删排序、删除确认、托盘动态名称、USB 学习/候选/关于页面及高 DPI 布局仍需实机交互验证。

验收：自动测试与 x64 Release 构建已通过；未执行真实 UDP、USB、蓝牙、DDC、唤醒或防火墙操作。DS-005 v2 网络运行时不在本阶段。

### W-005 文档与诊断安全

- [x] `Windows/README.md` 写清支持系统、Windows App Runtime 依赖和原生 DDC/CI 限制；DS-009 另明确当前不启用 ControlMyMonitor 回退。
- [x] 明确 GPU 驱动、转接器、扩展坞和显示器可能不透传 DDC/CI。
- [x] 诊断日志入口采用安全事件/数值字段白名单；配对密码、地址、路径、endpoint 和设备标识在写盘前移除。
- [x] “常规”页提供默认关闭且本机持久化的“详细诊断记录”；旧配置缺少字段时保持关闭，所有详细入口统一硬门控，双向切换均清空会话内存和磁盘旧轨迹。
- [x] “复制诊断”只能复制用户当前可见的只读预览，不存在绕过预览的导出路径。
- [x] 文档现说明 v2 PBKDF2/HMAC、重放与路由保护，并仍建议只在可信网络使用。

## P1：正式 Windows 分发

### W-101 安装包

2026-09-06：用户授权增加安装版，保留绿色版；可信签名继续延期。采用 Inno Setup、当前用户目录安装；按后续要求提供简体中文/英文，不内置运行库，安装前检测并提示用户从微软下载。未发布 Release 或修改版本。

- [x] 选择 Inno Setup 安装器方案，保留绿色版作为高级用户选项。
- [ ] 安装、升级和卸载不丢失用户配置；卸载残留策略写入文档。
- [ ] 登录启动在安装版和绿色版下都可控、可撤销。
- [ ] 建立可信代码签名方案；证书和密钥只放在安全的 CI secret/签名服务中。
- [ ] 在干净 Windows 用户环境验证安装、首次启动、升级和卸载。

安装生命周期模拟回归已加入 Windows CI，覆盖中英文提示、Bootstrap 参数与失败中止、重装、降级拒绝、运行中拒绝、配置保留和仅删除自身登录启动项。完整原生测试、x64 Release 与安装器打包结果以本任务 PR CI 为准；干净 Windows 上真实运行库安装、首次启动、升级和登录启动仍待实机确认。

### W-102 发布自动化

2026-09-09：用户确认 W-002 重新绑定修复实机测试通过，PR #88 已合并。授权发布 Windows v2.3.1 / build 22；macOS 保持 v2.3.0。此次只更新 Windows 版本资源与下载文档，自动构建、安装包版本和校验和结果以发布 PR/Release 为准，不扩大硬件兼容性验证范围。

2026-09-07：用户确认已完成实机测试并授权正式发布。v2.3.0 / build 21 在已合并 PR #86 上统一双端版本、发布文件名与下载说明；新版本自动构建和发布校验结果见发布 PR/Release。用户确认不扩展为对所有硬件和系统组合的兼容承诺。

- [ ] 版本号有唯一来源，二进制、安装包和 GitHub Release 保持一致。
- [ ] Release 构建生成校验和，并保留构建日志和依赖版本。
- [x] 安装包与 CI 产物使用产品、平台、架构和格式命名。

## P2：通用 3.0

### W-201 DDC 后端接口化

- [x] 定义统一的枚举、能力检测、VCP 读取/写入、错误、取消和后端可用性接口。
- [x] 通用 `IDdcBackend` 与控制服务保持接口化；DS-011 后正式后端集合只实例化原生 Dxva2，W-206 后普通接口只接受亮度、对比度和音量。
- [x] 每台显示器按稳定逻辑 ID 关联；设置页不再提供后端选择，原生 Dxva2 明确显示可用、不支持或暂时失败。
- [x] 亮度 `0x10`、对比度 `0x12`、音量 `0x62` 支持独立开关、显式读取和提交式滑块写入；联动所有显示器默认关闭。
- [x] 后端、显示器或单项失败相互隔离；全零遥测、估计缓存、异常最大值、安全状态、取消和迟到结果按 DS-004 语义处理。
- [x] 纯模拟后端覆盖 C-016 至 C-020、C-024、原生不可用/失败、稳定 ID 重排、显式联动部分失败和零调用安全门；当前继续通过 v2 与 USB 公共向量。
- [ ] 三项回读/提交、托盘滑杆、联动、取消、状态提示和高 DPI 布局仍需实机交互验证；真实 DDC 兼容性须另行授权测试。

### W-202 首次运行向导

- [ ] 引导选择 USB 设备、枚举显示器、识别每台显示器和填写输入源。
- [ ] 测试输入源前明确说明影响，并要求用户主动确认。
- [ ] 向导完成前保持自动切换关闭。

### W-203 诊断页面与脱敏日志

- [x] 显示应用版本/构建/架构、协议与 schema、安全状态、匿名协同在线/最后合法心跳、USB 状态、原生 Dxva2 接口能力及各显示器控制和最后操作状态；合法心跳按会话保留 `Never -> Recent -> Expired`，配置身份变化、删除或会话重置会清除。
- [x] 打开、刷新和复制只通过可注入的只读 snapshot provider 投影配置快照与内存状态；预览层无法获得 UDP、USB、唤醒、DDC 或输入源接口，既有“检测”保持独立，复制不会再次读取快照。
- [x] 预览严格移除配对密码、地址、endpoint、认证/消息标识、用户路径、显示器/USB 标识和用户名称，并以仅会话内 `P/D/S/O` 编号展示；复制文本与预览逐字一致。
- [x] 显示器最后操作按稳定逻辑 ID、物理绑定和 topology generation 管理；相同绑定重枚举保留，真实绑定/generation 变化清除，重排和歧义不串状态且不延长句柄生命周期；多项 DDC 批次按目标显示器聚合，任一失败、不可信或歧义不会被后续成功项覆盖。
- [x] 诊断预览始终可用并明确显示 `detailed-recording`；关闭时只投影配置、能力和基本状态且不泄漏旧轨迹，显示器页只展示简明成功/失败结果。
- [x] 用户完成本轮 Windows 测试包实机验收，确认诊断页、文本复制和多显示器状态展示可用。
- [ ] 新增“详细诊断记录”开关的即时保存、重启保持、双向切换清理及高 DPI 布局仍需实机验证；休眠恢复、热插拔和接口切换后的状态生命周期仍需单独实机验证。

### W-204 协议 v2/HMAC

- [x] 按当前生效的 `PROTOCOL.md`、已批准的 DS-005 提案与 `contracts/protocol-v2/` 实现 v2 HMAC、路由与状态机；DS-007 后正式运行时只接受整数 version 2。
- [x] 使用 Windows CNG 实现 PBKDF2-HMAC-SHA256、规范化认证输入、常量时间校验、nonce/时间窗和重放防护。
- [x] Windows 原生测试接入共享 v2 向量，覆盖 1 条 NFC、4 条认证、22 条消息和当前 6 条状态机向量；不再引用 `contracts/protocol-v1/`。
- [x] 协同配置“检测”只发送 v2 探测，只接受来源地址、端口、配对码和待处理 `eventID` 唯一匹配的认证响应；首次或变化的对端路由自动原子保存。
- [x] 检测过程阻断 USB、蓝牙、唤醒与 DDC；常规 v2 `status_response` 也必须匹配尚未过期的待处理探测后才能刷新在线状态。
- [x] 双方 `peerEndpointID` 均为空时，接收端按数据报来源 host/port、配对凭据和唯一配置匹配 `targetEndpointID = null` 的首次探测；歧义、认证失败或 endpoint 冲突均零回复、零硬件副作用。

### W-205 自动发现

- [ ] 在安全模型确定后再做 Bonjour/mDNS 发现。
- [ ] 自动发现只提供候选设备，不自动信任、不自动交换配对码、不触发硬件动作。

### W-206 输入源切换与显示器 DDC 调节后端解耦

- [x] 输入源切换使用独立 `IInputSourceTransport` 和 `InputSourceSwitchService`；USB 离开及协同切屏不再依赖或调用 `IDdcBackend::Write()`。
- [x] `DdcVcpCode` 只保留亮度、对比度和音量；普通 DDC 服务与原生 Read/Write 共用同一份三项白名单，在显示器解析和 DXVA2 调用前拒绝其他 VCP 项；输入源值只存在于原生输入源传输内部。
- [x] 原生 DDC 与输入源传输共享同一个物理显示器会话、句柄集合、拓扑 generation、解析缓存和串行锁，没有复制枚举或绑定逻辑。
- [x] 输入源保持一次刷新重试、取消/安全门、歧义与离线零写入、拓扑变化丢弃旧结果及多显示器失败隔离；受控刷新递增 generation 后接受携带新 generation 的成功结果，但仍拒绝落后于当前拓扑的迟到结果。
- [x] 自动测试使用相互独立的 DDC fake 和输入源 fake，覆盖 service/native 三项白名单、受控刷新后成功、外部 generation 变化丢弃、调用隔离、多显示器、失败隔离、取消、离线和歧义。
- [ ] 真实 USB 与协同输入源切换、一次刷新重试及拓扑变化行为仍需用户明确授权后实机验证。

### Windows 输入源 null/零值安全与 USB 保存回显

- [x] USB 与协同输入源统一为 `null` 或 `1...65535`；空白保存为空映射，显式 `0`、负数、非数字和溢出值在 UI 与配置校验中拒绝，空映射回显为空白。
- [x] 旧 v5 配置中的 USB/协同零值原子迁移为空映射；全部为空时相关功能关闭，迁移失败保留原文件并持久进入零副作用安全状态。
- [x] 协同配置只要求至少一台当前显示器具有有效映射；部分空映射报告 `missing_mapping`，其他显示器继续执行。
- [x] Controller、USB 协调器、输入源切换服务和原生 DXVA2 最终传输边界逐层拒绝零值，确保不会调用 `SetVCPFeature(0x60, 0)`。
- [x] USB 页使用独立保存反馈作用域：仅用户修改并成功持久化后短暂显示绿色“✓ 已保存”；页面/配置/重载只清除成功态，失败态保持，非保存操作不触发且不覆盖。
- [x] `DS-026` 共享契约提案已于 2026-09-02 批准；`contracts/usb-switch-v1` 的映射和动作范围已统一收紧为 `1...65535` 或 `null`，双端缺失映射继续使用 `missing_mapping`；未修改协议或 macOS 源码。
- [x] 纯自动测试覆盖输入边界、零值迁移及失败安全、全空/部分映射、USB/手动/协同入口、最终传输边界和保存反馈隔离；完整 316 checks 与 x64 Release 通过。
- [ ] 真实设置页空白/非法值回滚、USB/协同部分映射和保存反馈时序仍需用户实机验证；未执行真实输入源、DDC、USB、网络或唤醒操作。

### Windows RDP 会话显示拓扑安全

- [x] 将持久物理显示器目录与当前会话实时拓扑分离；只有完整、非空、可信的本地物理快照允许协调和保存。
- [x] 枚举可信度明确区分本地权威、远程会话受限和不完整/不可用；远程、镜像、访问受限、失败、部分和空结果均保留现有目录与全部 USB/协同映射。
- [x] 远程会话使用 `SM_REMOTESESSION`、当前进程 session 与 `GlassSessionId` 判断，并按设备标志排除远程和镜像目标，不依赖设备名称黑名单。
- [x] RDP 期间阻断 DDC 读取/写入和输入源切换，不新增虚拟显示器、不保存远程拓扑；返回本地后复用拓扑失效路径并响应 WTS 会话变化重新枚举。
- [x] 纯模拟测试覆盖本地 3 台→RDP 虚拟目标→本地重排恢复、首次 RDP 启动、远程/镜像标志、访问受限、失败/空/部分结果、零保存和零硬件调用。
- [ ] 真实 RDP 连接、RDP 断开返回本地、物理显示器及映射恢复和恢复后的 DDC/输入源操作仍需用户明确授权后实机验证。

### DS-027 联动 DDC 统一控件

- [x] `LinkAllDisplays=false` 保留设置页和托盘的逐显示器结构；开启后亮度、对比度、音量各最多投影一个公共滑杆，不改写每台显示器的功能和托盘偏好。
- [x] 设置页和托盘共用纯 `DdcProjectedControl` 模型；同值显示具体值，不同值显示“混合”，任一目标缺少可信值显示“—”，未开始用户调节前不使用平均值或单台值冒充公共值。
- [x] 公共最大值取全部合格目标有效上限的最小值，未知上限按 100 参与；服务写入前再批量预检，超界时保持零 native transport 调用。
- [x] 实际目标只包含已启用对应功能、当前本地会话在线且唯一解析的物理显示器；RDP、远程/镜像、歧义、离线或不完整拓扑在投影与最终写入入口都为零目标。
- [x] 托盘联动模式按功能扁平展示；显示条件只需任一已配置显示器同时开启功能与托盘，触发后写所有已开启该功能的合格目标。
- [x] 功能、托盘和联动开关仍使用原子即时保存，成功后设置与托盘立即重建；DDC 操作不进入保存反馈作用域。
- [x] 纯 fake 回归覆盖开/关、0/1/多显示器、同值/混合/未知、功能/托盘组合、离线/歧义/RDP/不可信拓扑、上限交集、批量绝对写入、部分失败、取消/generation 与关闭联动恢复。
- [ ] 真实多显示器的公共滑杆、混合/未知显示、托盘扁平布局、部分故障和 RDP 往返后恢复仍需用户授权后实机验证。

### DS-028 Windows 托盘与设置窗口行为统一

- [x] 托盘顶部只读状态仅投影持久化“USB 切换”开关并表达“已开启/已关闭”，不再显示设备名称、VID/PID 或设备路径；RDP、安全模式、学习和拓扑信任只限制运行能力，不伪装成配置已关闭。
- [x] USB、协同切换、亮度、对比度、音量、设置和退出菜单项均使用语义图标；字体不可用时退化为普通项目符号，分隔线无图标。
- [x] 自绘菜单统一由可测试布局模型计算图标、标签、滑杆和值列；W-033 后 96 DPI 最小视觉宽度统一为 260 像素，并按文本和 DPI 扩展而不截断。
- [x] 托盘左键、右键、双击和键盘激活均只打开同一菜单；只有明确选择“设置…”才打开设置窗口。
- [x] 设置窗口复用同一实例，菜单打开时恢复、激活并请求前台；不使用置顶、失焦隐藏或失焦销毁。
- [ ] 亮色/暗色、100%/150%/200% DPI、自绘图标退化和重复打开设置窗口仍需 Windows 实机视觉验证。

### DS-029 离线显示器安全删除

- [x] 重新检测只更新持久目录的在线/离线绑定状态，不自动删除目录或映射。
- [x] 删除入口只在最近一次非空 `LocalPhysicalAuthoritative` 检测明确标记为 `Offline` 时出现；远程、镜像、失败、空或不完整快照均隐藏并在执行时再次拒绝。
- [x] 删除前要求用户确认，并在确认回调中再次核验拓扑信任与离线状态；判断不使用显示器名称。
- [x] 候选配置一次性级联删除显示器目录、USB/全部协同映射、DDC 缓存和托盘偏好；只有原子持久化成功才替换内存配置，失败完整回滚。
- [x] 删除后没有有效 USB 映射会关闭 USB；协同配置没有有效显示器映射会停用；仍有部分有效映射时继续保留启用状态。
- [x] 删除纯配置且不调用 DDC、输入源、USB、网络或唤醒；模拟测试覆盖信任门、级联、部分映射、最后映射与失败回滚候选。
- [ ] 短暂离线物理显示器、RDP 往返后旧虚拟条目和保存失败提示仍需 Windows 实机 UI 验证。

### W-030 Windows 重启后 USB 自动切换恢复

- [x] 根因不是配置丢失：`UsbWatcher` 在恢复配置前已以无效绑定启动，旧回调不带配置代次；随后又在 `UsbSwitchCoordinator` 创建/更新前重配，排队的旧观察可能污染或丢失新绑定基线，切换开关因触发新观察而暂时恢复。
- [x] watcher 回调携带显式配置 generation；控制器只接受当前 generation，读取期间发生重配的观察在回调前废弃。
- [x] 每次应用配置先按当前可信拓扑构建/更新协调器，再重配同一个 watcher；冷启动有效绑定必产生一次当前代次观察，初始在场或离场都只建立基线。
- [x] RDP、远程/镜像、空/部分/失败快照和配置安全模式把 USB 协调器置为安全态；返回本地权威拓扑后先重建基线，期间零 DDC、输入源、网络或唤醒动作。
- [x] 同绑定重复初始化和配置 reload 保留协调器基线且不制造重复事件；绑定变化重建基线；既有防抖、唤醒合并、取消、side-effect generation 和部分失败隔离保持不变。
- [x] 纯模型测试覆盖 enabled/disabled、初始在/不在、拓扑先/后、RDP/不可信/安全模式、过期 watcher 回调、重复初始化、reload、换绑及部分映射。
- [ ] Windows 冷启动、USB 初始在/不在、RDP 往返和之后首次真实插拔仍需实机验证；不以启动模拟 toggle 或定时重开 watcher 代替。

### W-031 Windows 通知区域自适应单色图标

- [x] Windows Shell 通知区域使用独立的透明单色“显示器 + 百分号”线稿；应用资源、EXE、设置窗口和任务栏继续使用现有彩色品牌图标。
- [x] 图标颜色只读取 `SystemUsesLightTheme`：浅色任务栏用黑色，深色任务栏用白色；读取失败或高对比模式按系统背景亮度选择确定性的高对比色，不混用应用内容主题。
- [x] 隐藏托盘窗口监听系统主题、颜色和 DPI 变化；只有颜色或像素尺寸实际变化时才用一次 `NIM_MODIFY` 原位替换同一通知项，不删除重建或制造重复图标。
- [x] 16/20/24/32 像素使用同一可复现软件渲染源稿，主体约占槽位 90%（保留约 10% 光学留白），统一笔画为主体的 6%；纯测试校验透明边界、不裁边、黑白预乘像素、可见/alpha 覆盖率上下界和 100%/125%/150%/200% DPI 映射。
- [x] 动态创建的 `HICON` 在成功替换后才释放旧句柄，失败回滚；共享 fallback 不销毁，析构和构造失败路径释放全部自有资源。
- [x] 纯逻辑回归覆盖浅色、深色、未知主题 fallback、同状态不刷新及一次主题变化只产生一次有效更新；未修改托盘菜单、设置窗口、USB、DDC、会话或协议行为。
- [ ] 任务栏浅/深色、高对比、展开/折叠区及 100%/125%/150%/200% DPI 清晰度仍需 Windows 实机视觉确认。

### W-032 Windows 冷启动后首次输入源切换恢复

- [x] 根因是输入源动作直接复用启动时的单次物理拓扑快照和 DXVA2 句柄租约；新旧进程交接或显卡拓扑尚未稳定时，某台显示器可被保留为离线/未解析，手动切换便会跳过它；关开 USB 只是通过重新应用配置偶然刷新了该运行态。
- [x] 手动协同和 USB 离开动作在真正切换前各只获取一次当前物理拓扑，并只在本地非空权威快照上生成本次动作计划。
- [x] 动作计划只更新内存副本的运行态绑定，不写回 `config_` 或 `settings.json`；可信拓扑中单屏离线/歧义/缺映射仍只隔离本屏，其他有效显示器继续。
- [x] 强制枚举即使物理拓扑指纹不变也替换旧 DXVA2 句柄租约，但不伪造 topology generation；真实拓扑变化仍递增 generation 并丢弃旧结果。
- [x] RDP/虚拟、访问受限、部分、空或失败快照仍零输入源写入；配置重载改变 side-effect generation 后，枚举所得过期计划在首屏前零写入，批处理中途则在下一台显示器前停止。
- [x] 纯 fake 回归覆盖冷启动/升级后过期绑定、USB 开关不应影响手动切换、两屏单屏离线隔离、RDP/不可信零写入、配置并发取消和句柄租约刷新决策。
- [ ] 退出旧版后立即启动新版、首次手动切换、首次 USB 离开、两屏部分失败及 RDP 返回本地仍需实机验证。

### W-033 Windows 托盘菜单图标与 DDC 行紧凑化

- [x] “退出”不再使用视觉重量突兀的关闭 X，改用与 USB、协同、DDC 和设置项相同 Segoe Fluent/MDL2 字体、字号与字重的标准电源图标；字体不可用时继续安全退化为普通项目符号。
- [x] 根因是 DDC 标签列固定为 64 像素、两侧间距固定为 8 像素且含滑杆时强制 272 像素目标宽度；短标签因此产生无意义空白，长标签仍可能被固定列裁切。
- [x] 标签列改为按实际最宽 DDC 名称动态测量，最小 32 像素；滑杆间距缩为 4 像素，菜单统一以 260 像素为最小视觉宽度，只在文本或滑杆最小可操作宽度要求时扩展。
- [x] 生产布局和测试共用同一个几何模型；纯测试覆盖电源字形、短/长标签、中文名称、混合/三位数值空间，以及 100%/125%/150%/200% DPI 下不裁切和滑杆最小宽度。
- [x] 综合 PR #78 / Windows run `33823153891` 已完成 x64 Release、411 checks、framework-dependent 分发校验和 artifact 上传；未执行真实 DDC、USB、网络、唤醒或输入源操作。
- [ ] 暗/亮菜单和 100%/125%/150%/200% DPI 实机视觉仍需用户验收。

### W-034 Windows 后台媒体键 DDC 路由

- [x] 隐藏托盘窗口以 `RIDEV_INPUTSINK` 观察键盘最终发出的媒体动作且不设置 `RIDEV_NOLEGACY`；标准音量增减/静音接受键盘 VK 或 HID Consumer Control usage，亮度只接受标准 HID usage；30ms 内相同动作的跨来源双上报只分发一次，同来源/超窗重复保留。不监听 Fn/F 键、不猜 OEM 键码、不使用低级 hook，也不吞系统原生动作。
- [x] 每次媒体动作先强制刷新当前物理拓扑及 DXVA2 句柄租约；只有本地权威、非空快照进入规划，RDP、远程/虚拟、部分、空、失败和安全配置在路由层零目标，`DdcControlService` 最终入口继续执行同一可信拓扑门控。
- [x] 未联动时向每台启用对应功能、在线且唯一解析并具有可信当前值的显示器执行相同步进 5，保留显示器间差值；未知值仅隔离该屏。联动时沿用公共绝对值，只在公共值确定时执行；混合或任一未知值零写入。
- [x] 静音只处理启用音量且具有可信值的合格目标；每台最近非零值仅保留于当前配置 generation 的进程内存，写 0 后恢复；配置重载、安全门重置或退出清除旧恢复值。联动恢复要求全部目标存在相同安全恢复值，禁止持久化或猜测。
- [x] 写入复用 `Controller::SubmitDdcWrite`、`DdcWriteQueue` latest-wins、一次刷新重试、side-effect generation、取消和动作时配置副本；失败清除未提交的乐观步进，配置重载/拓扑变化/退出清空待处理动作。
- [x] 纯 fake 测试覆盖音量、静音、标准亮度 HID、释放/非标准键拒绝、重复事件、联动开关、同值/混合/未知、离线/RDP/不可信/缺目标、配置 generation、冷启动首次事件和失败后基准恢复。
- [x] 综合 PR #78 / Windows run `33823153891` 已完成 x64 Release、411 checks、framework-dependent 分发校验和 artifact 上传；artifact `9919244404` 为 9 文件、902507 字节。
- [ ] 仍需 Windows 实机验证不同键盘是否产生标准事件、按住重复、Windows 原生动作不受影响、双屏步进/静音、RDP 与会话切换；自动验证未执行真实 DDC、USB、网络、唤醒或输入源操作。

### DS-007 设置、DDC 与 v2-only 收敛

- [x] 正式 Windows 运行时删除 v1 解析、状态机、发送、心跳和探测回退；version 1、缺失、类型错误和未知版本零回复、零在线刷新、零硬件副作用。
- [x] 本机配置升级为 schema v4；v3 或更早配置先保留递增后缀备份，再创建协同、USB 和 DDC 写入均关闭的安全默认值，不迁移旧字段。
- [x] 设置页统一为“常规 / USB 切换 / 协同 / 显示器 / 关于”，删除全局保存/取消并实现成功后更新运行时的即时原子保存、非法文本回滚和删除确认。
- [x] DS-007 当时的 USB 页按协同配置选择或学习设备；DS-008 已将其替换为独立本机设备、显示器输入映射和可选联动目标。
- [x] 独立关于页只读取公开元数据并保持零配置、网络和硬件副作用。
- [x] 显示器页使用全局控制通道与联动开关；每台显示器只显示读取按钮和亮度/对比度/音量的功能开关、托盘开关、滑杆和值。
- [x] 托盘只投影同时启用且允许托盘显示的项目，并提供真实滑杆；连续写入按显示器和项目 latest-wins 合并、串行执行，原生失败仅重试一次。
- [x] 模拟测试覆盖 Windows 适用的 DS-007 保存、v4、安全门、版本拒绝、关于、托盘、DDC 零值/隔离/联动/取消场景及全部 v2 公共向量。
- [ ] 六页紧凑布局、窄窗口/高 DPI、键盘焦点与辅助功能、托盘滑杆，以及真实 DDC/USB/v2 跨设备行为仍需实机验证。

### DS-008 本机 USB 双向切换

- [x] 配置升级到 schema v5；v4 文件先保留 `.pre-v5.backup`，再原子迁移并完整回读，保留非 USB 设置和协同配置，不从旧触发设备或协同映射猜测新 USB 数据。
- [x] USB 切换与 V2 状态机完全解耦，只监听用户明确选择的一个本机设备；首次观察和配置重载后只建立基线。
- [x] USB 接入变离开时独立异步提交每台合法映射的输入源 DDC，零网络等待；离开变接入时只请求本机显示器唤醒。
- [x] “联动协同”默认关闭；开启后与 DDC 同时调度一次认证 `wake_display`，网络失败不阻断、不推迟、不回滚 DDC。
- [x] 合法 `wake_display` 只唤醒、不回复、不写 DDC、不进入手动协同状态机；网络唤醒和 USB 接入共用 2 秒合并器。
- [x] USB 页提供独立设备、每显示器目标输入源、USB 开关、联动开关和联动目标配置；普通界面不显示原始 VID/PID 或设备路径。
- [x] 原生自动测试消费 `contracts/usb-switch-v1/` 的 USB-001 至 USB-016，并继续通过 v2 的 1+4+20+6 公共向量；全部使用模拟状态和临时配置。
- [ ] 单设备断开/接入、每显示器映射、并行联动唤醒、2 秒合并、设置页即时保存和高 DPI 布局仍需授权后的双机/实机验证。

### W-009 / DS-009 显示器身份与原生 DDC 稳定性

- [x] W-009 建立了原生实时枚举；DS-013 进一步改为“逻辑显示器→当前唯一物理 DDC 句柄”两层解析，断开项只标记离线，不再删除显示器配置、协同映射或 USB 映射。
- [x] 设置页、USB 映射和托盘使用保存的用户名称或系统友好名称；同型号按稳定本机 ID 排序生成仅本机可见序号，不向日志、测试或交接记录写入真实硬件标识。
- [x] W-009 运行时固定使用 Windows 原生 Dxva2；DS-011 进一步删除外部工具后端、进程执行、路径检测、后端选择和兼容配置字段。
- [x] 原生枚举、能力检查、VCP 读取和写入明确传播失败；句柄集合统一去重和释放，Dxva2 调用串行化。明确失败后只重新枚举一次，不使用固定 sleep、成功后重试或无限重试。
- [x] USB 学习同时识别全新设备和“学习开始时已存在、离开后重新接入”的设备；多候选、取消、30 秒超时和配置变化继续保留原绑定并维持零硬件/网络副作用。
- [x] 纯模拟测试覆盖 2 台不重复为 4 台、枚举重排/断开/重接、友好名称、原生明确失败、部分失败、句柄释放，以及 USB 初始存在/离开/重接/多候选/取消/超时；既有 v2 与 USB 公共向量继续通过。
- [x] 未绑定但本机字段完整的 v2 配置通过独立 bootstrap 候选启动 UDP 监听；双方未绑定或接收方唯一绑定请求方时均可安全响应空目标 `status_probe`。bootstrap 不进入正常协同、菜单或硬件路径，也不自动启用配置或保存 endpoint。
- [ ] 实际两台显示器枚举数量、系统名称、同型号区分、连续原生 DDC 读取/写入、输入源切换和 USB 离开重接学习仍需用户安装测试包后验证。

### DS-011 Windows 原生 DDC 单后端清理

- [x] 正式后端集合只实例化原生 Dxva2；删除外部 DDC 工具后端、进程启动/等待/退出码处理、路径检测和回退入口。
- [x] 删除顶层后端选择、外部工具路径、每显示器兼容路径及设置页控制通道残留；schemaVersion 保持 5，旧配置多余字段作为未知字段忽略且后续保存不再写回。
- [x] 保留 `IDdcBackend`、`DdcControlService`、稳定显示器身份、一次刷新重试、取消、高频合并、批处理、缓存和安全门。
- [x] 自动测试验证正式集合只有原生后端、旧字段剥离、原生不可用/读写失败明确传播，以及既有 DDC、输入源、USB 和协议回归。
- [ ] 原生 Dxva2 枚举、三项 VCP 读取/写入和输入源切换仍需经用户明确授权后实机验证。

### DS-012 Windows 协同检测非阻塞化

- [x] “检测”发送的 PBKDF2、地址解析和 UDP 发送改为可取消后台操作；WinUI 线程只发起操作和接收代次仍有效的结果。
- [x] 入站 v2 消息在后台完成 PBKDF2/HMAC 验证，再把已验证消息交回 UI 线程串行推进状态机。
- [x] 派生密钥使用容量受限的线程安全内存缓存；配对密码或 endpoint 变化产生独立条目，配置代次和检测会话变化清空缓存。
- [x] 检测按钮在运行期间禁用并显示“正在检测”；连续点击、配置切换、保存、窗口关闭、超时和迟到结果均由代次与取消门阻断。
- [x] 恢复此前遗漏在已合并基线之外的紧凑布局：USB 页拆为设备、输入源和联动三块；协同页拆为状态/操作、配置选择和配置内容三块，并删除卡片内的检测、上移和下移按钮。
- [x] “检查网络权限”与“检测连接”保持独立；未绑定草稿只有用户主动检查后才监听，已完成并启用的配置在后续启动时继续正常监听。
- [x] 探测响应的 2 秒窗口从 UDP 数据报确认发送成功后开始，不再被后台 PBKDF2 和地址解析提前消耗；地址解析或发送失败显示独立错误，并以不含配置和身份数据的阶段日志辅助实机诊断。
- [x] 自动测试注入慢 KDF 和慢解析/发送工作，验证调用立即返回、完成经 dispatcher 回调、取消结果不落地、缓存命中/容量/失效正确；既有 v2、USB、DDC 和 bootstrap 回归继续通过。
- [x] 用户实机确认单击检测不再卡死，WinUI 保持响应且真实局域网检测通过；检测过程未执行硬件动作。

### W-013 / DS-013 动态显示器物理绑定

- [x] 使用 `QueryDisplayConfig` / `DisplayConfigGetDeviceInfo` 建立当前逻辑 target 快照，结合 SetupAPI 的当前接口、Container ID 和 EDID 生成仅本机保存的不透明强身份。
- [x] 一个逻辑 target 只投影一个设置/托盘项；多个物理句柄、身份缺失、重复强身份或多候选迁移均标记歧义/需重新确认，亮度、对比度、音量和输入源保持零 DDC。
- [x] 全局一对一解析不依赖 FriendlyName、品牌、型号或枚举顺序；旧 GDI/接口标识只在存在唯一非歧义强候选时迁移。
- [x] 当前拓扑使用 generation；`WM_DISPLAYCHANGE`、主动刷新、枚举差异或句柄失效会释放旧句柄并使解析缓存失效。generation 变化后即使底层返回成功，也丢弃该结果并停止剩余批量操作。
- [x] 模拟测试覆盖 2 个逻辑 target/4 个底层句柄、歧义零 DDC、同型号无序列号、强身份一对一、重排、接口切换、部分/空枚举、休眠恢复、旧句柄迟到成功、迁移和映射保留。
- [ ] 实际两台显示器只显示两项、同型号不串台、热插拔/接口切换后重绑定，以及四类 DDC 操作的实机稳定性待用户授权验证。

## 当前不要做

- 不要修改或回退 macOS 源码来迁就 Windows 的旧模型。
- 不要从 C++/WinUI 3 改回 C#，也不要新建第三套 Windows 实现。
- 不要恢复协议 v1 运行时或添加只有单端认识的协议字段或消息。
- 不要为“提速”删除手动协同的 150 ms 重发、消息校验、重放保护或超时兜底；USB 本机路径按 DS-008 不增加网络等待。
- 未经用户明确确认，不要执行真实显示器输入源切换、真实 USB 交接、睡眠/唤醒测试或系统防火墙修改。
- 不要提交 `bin/`、`obj/`、`dist/`、个人证书、签名密钥、配对码、本机配置或诊断日志。

## 进度记录

| 日期 | 清单项 | 状态 | 提交 SHA | 验证说明 |
| --- | --- | --- | --- | --- |
| 2026-08-27 | 建立 Windows 跨设备开发清单 | 完成 | 36aa59b | 仅文档，不修改 Windows 代码 |
| 2026-08-27 | W-001 清除个人硬件默认值 | 代码完成；实机验收待确认 | 980befd | x64 Release 构建通过；dist 1.24 MiB；源码与产物个人默认值扫描通过；未启动新版或执行硬件动作 |
| 2026-08-27 | W-002 动态多显示器模型 | 代码已完成；设置页和高 DPI 实机验证待确认 | 46ecfd0 | 0/1/2/3/4 台、UUID/枚举顺序、旧配置迁移、增删重连和单台失败隔离测试通过；未启动新版或执行硬件动作；PR #2 |
| 2026-08-27 | W-003 交接状态机可测试化 | 完成 | c4acaf8 | 17 组消息向量和 16 组状态机向量通过；x64 Release 构建通过；未执行真实硬件动作；PR #5 |
| 2026-08-27 | W-004 Windows CI | 完成 | 4ffd077 | `windows-2025-vs2026` 全新托管 runner 构建、17+16 向量测试、dist 结构与小于 20 MiB 检查、artifact 上传均通过；PR #9 / run #1 |
| 2026-08-27 | DS-004 第一阶段本机模型与 UI | 代码完成；实机交互待确认 | a6c9a7b | schema v3、配置迁移/安全状态、多协同配置 UI、动态托盘菜单和本机检测完成；C-001..C-015 与 v1 17+16 向量通过；未执行真实硬件或网络测试 |
| 2026-08-27 | DS-004 正常设置保存失败安全化 | 代码完成；实机交互待确认 | bc6b234 | 编码/临时写入/合法但不一致的完整回读/原子替换故障测试通过；旧文件和安全标记跨重启保持，当前实例阻断 UDP、USB、DDC、唤醒，合法保存后解除；未执行真实硬件或网络测试 |
| 2026-08-28 | W-201 DDC 后端接口化与三项控制 | 代码完成；实机交互待确认 | 9e078a4 | Dxva2/CMM 独立后端、每显示器后端、亮度/对比度/音量、回读/缓存/显式联动/取消完成；C-016..C-020、C-024 与 v1 17+16 向量通过；x64 Release dist 1.44 MiB；未执行真实硬件或网络测试 |
| 2026-08-28 | DS-006 Windows 公开文档清理 | 完成 | 69c867c | 清除开发清单中的具体旧 USB VID/PID；README 对齐 schema v3、多协同配置、角色中性名称与 W-201 DDC 控制；保留旧 C# 迁移兼容说明；文档检查和 x64 Release 自动测试通过，未执行真实硬件或网络操作 |
| 2026-08-28 | DS-004 Windows USB 学习与关于页面收尾 | 代码完成；实机交互待确认 | 8b525b2 | 稳定配置 ID、30 秒基线学习、多候选确认、取消/删除/超时/迟到保护、学习期零副作用及公开关于页完成；C-021..C-023、既有 C-001..C-020/C-024 与 v1 17+16 通过；x64 Release dist 1.49 MiB；未执行真实硬件或网络测试 |
| 2026-08-28 | W-204 协议 v2/HMAC | 历史实现已完成；v1 兼容随后由 DS-007 删除 | 本分支当前实现 | 当时完成 v1/v2 分派、PBKDF2/HMAC、endpoint 路由、重放防护和三条协同路径；当前验收状态以 DS-007 行为准 |
| 2026-08-28 | DS-007 Windows 设置、DDC 与 v2-only 收敛 | 代码完成；实机交互待确认 | 本分支当前实现 | schema v4 安全重建、五页即时保存、全局 DDC 通道、托盘滑杆/latest-wins、v2-only 和 v2 1+4+20+18 回归完成；x64 Release dist 1.58 MiB；未执行真实网络或硬件测试 |
| 2026-08-29 | DS-008 Windows 本机 USB 切换 | 代码完成；实机交互待确认 | 本分支当前实现 | schema v4→v5 原子迁移、独立单设备监听、离开立即 DDC、接入本机唤醒、可选单次认证 `wake_display` 和 2 秒合并完成；USB-001..USB-016 与 v2 1+4+20+6 通过；未执行真实网络或硬件测试 |
| 2026-08-29 | W-009 / DS-009 原生显示器控制 | 代码完成；实机兼容性待确认 | fc95ea3 | 实时集合去重/清理、友好名称、Dxva2 串行句柄生命周期、原生失败零 CMM 和 USB 离开后重接学习完成；x64 Release 与全部模拟回归通过；未执行真实网络或硬件测试 |
| 2026-08-29 | DS-009 Windows 非对称协同 bootstrap | 代码完成；双机网络待确认 | 0392969 | 双方未绑定、接收方单边绑定、双方绑定和身份冲突均有测试；真实 loopback `UdpPeer` 固定源端口往返、x64 Release、v2 1+4+20+6 和 USB-001..016 通过；未执行真实局域网或硬件测试 |
| 2026-08-30 | DS-011 Windows 原生 DDC 单后端清理 | 代码完成；实机兼容性待确认 | 本分支当前实现 | 正式实现删除外部工具后端、进程执行、路径/选择/兼容字段和设置页残留；x64 Release、全部模拟测试、v2 1+4+20+6 与 USB-001..016 通过；dist 1.61 MiB；未执行真实硬件操作 |
| 2026-08-30 | DS-012 Windows 协同检测非阻塞化 | 代码完成；真实局域网与 UI 响应待确认 | 5068aae | 检测发送和入站认证移出 UI 线程；有界派生密钥缓存、代次取消、窗口生命周期和按钮忙碌态完成；慢 KDF/解析模拟、全部自动测试及 x64 Release 构建通过；未执行真实网络或硬件操作 |
| 2026-08-30 | DS-012 设置布局恢复 | 代码完成；实机布局待确认 | d14ce5b | 从遗漏分支选择性移植 USB/协同三段卡片、顶部网络检查与检测入口、删除无效排序按钮；保留 DS-012 后台认证与取消模型；自动测试和 Release 编译通过，正式 dist 覆盖因旧版进程占用而未完成，另生成隔离测试目录 |
| 2026-08-30 | W-005 / W-203 文档与诊断安全 | 代码完成；实机交互待确认 | 本分支实现提交 | 严格日志白名单、六页诊断预览/同文复制、匿名运行状态和按逻辑显示器+绑定+generation 的最后操作生命周期完成；全部模拟回归和 x64 Release 通过，未执行真实网络或硬件操作 |
| 2026-08-30 | DS-012 检测响应窗口修复 | 代码完成；真实局域网待确认 | c667cdd | 2 秒响应窗口改为从 UDP 实际发送成功后开始；发送失败与无响应分流；慢 KDF/解析后的完整响应窗口、取消发送和零硬件副作用回归通过；完整 x64 Release 构建及测试通过 |
| 2026-08-30 | W-013 / DS-013 动态显示器物理绑定 | 代码完成；实机物理绑定待确认 | 8b01943 | 两层拓扑、强身份一对一、歧义零 DDC、generation 失效和断开配置/映射保留模拟回归通过；x64 Release 与 v2 1+4+20+6、USB-001..016 通过；dist 1.66 MiB；未执行真实硬件操作 |
| 2026-08-31 | Windows 按需详细诊断记录 | 代码完成；实机 UI 待确认 | 本分支实现提交 | 默认关闭和旧配置安全默认、持久开关、全入口硬门控、双向切换清空、关闭时摘要预览与简明 DDC 状态完成；246 checks 和 x64 Release 通过；未执行真实网络或硬件操作 |
| 2026-08-31 | PR #54 / #60 集成与用户验收 | 完成；专项热插拔场景待验 | e14ae6e、3a22c66 | 两个 PR 均已合并；Windows CI runs 33366897393、33367712427 全绿，用户确认最终测试包、诊断页和协同检测通过且检测不再卡死 |
| 2026-08-31 | DS-021 发布准备事实同步 | 文档完成；无运行时变更 | 本任务提交 | 同步原生-only、v2-only、六页诊断与绿色测试包边界；未关闭休眠恢复、热插拔、接口切换、高 DPI 和未覆盖 DDC 场景 |
| 2026-08-31 | W-206 输入源传输解耦 | 代码完成；实机切屏待确认 | PR #66 | 普通 DDC 仅保留三项 VCP，输入源改用独立 transport/service；评审后补齐 service/native 共用白名单和受控刷新 generation 回归；254 checks、x64 Release 通过；未执行真实硬件操作 |
| 2026-09-01 | Windows RDP 会话显示拓扑安全 | 代码完成；RDP→本地实机恢复待确认 | 本分支当前实现 | 本地权威/远程受限/不完整枚举分类、远程及镜像目标排除、RDP 零持久目录变更与零 DDC、WTS 返回本地刷新及三显示器往返模拟回归完成；262 checks 和 x64 Release 通过，未连接真实 RDP 或执行硬件操作 |
| 2026-09-01 | Windows USB/协同 UI 信息架构对齐 | 代码完成；实机布局待确认 | 本任务提交 | USB 页合并为“自动切换/联动协同”两张外层卡片；协同页合并为“协同状态/配置”两张外层卡片且配置详情无嵌套卡片；保存与操作反馈分离、成功反馈绿色且模拟 2 秒自动隐藏。完整自动测试实际通过 285 checks；x64 Release 构建、测试和 dist 打包通过，绿色版目录 1.76 MiB；不执行真实网络或硬件操作。 |
| 2026-09-01 | PR #69 P1 审核修复 | 代码完成；设置窗口首开待实机确认 | 本任务后续提交 | USB 设备行不再接收或挂载状态 TextBlock，当前状态只属于独立状态行；保存 API 强制使用 None/Collaboration 范围，非协同保存不显示或清除协同保存反馈。完整模拟回归、x64 Release 和 dist 打包通过 287 checks；已启动现有配置下的托盘进程确认稳定，但为保留用户数据未强制删除配置触发首开设置窗口。 |
| 2026-09-01 | PR #69 P1 验证闭环 | 代码完成；设置窗口、主题和 DPI 实机验收待确认 | 本任务后续提交 | 生产 BuildContent 直接消费 USB/协同布局模型并为实际卡片设置自动化名称；保存反馈控制器统一处理 Collaboration/None、无变化、失败、两秒超时与协同页可见性，测试驱动此生产控制器。完整模拟回归 291 checks、x64 Release 和 dist 打包通过；应用以托盘进程启动且响应正常，但 UI Automation 未能枚举托盘入口，未为验证而改动用户配置或执行硬件/网络操作。 |
| 2026-09-01 | PR #69 第二轮审核修复与 UI 冒烟 | 代码和页面冒烟完成；主题切换与多 DPI 专项待确认 | 本任务后续提交 | 网络权限结果按 ready 选择成功/失败 severity；USB 学习使用显式 completion 区分成功、取消、超时、失效和失败；协同保存失败立即停止成功计时器。生产布局 Presenter 强制 USB 状态唯一父节点及协同保存反馈固定底部。完整模拟回归 293 checks、x64 Release 和 dist 打包通过；从 Release 包以 `--show-settings` 实际打开设置窗口并依次进入 USB/协同页，两页均成功构建、非空、各两张主卡片且无崩溃。未点击网络、USB、DDC、输入源或唤醒操作。 |
| 2026-09-02 | Windows 输入源 null/零值安全与 USB 保存回显 | 代码完成；实机交互待确认 | bf0d8f8 | 配置、UI、USB/协同调度、切换服务及 DXVA2 最终边界共同阻断 0；旧零值原子迁移为空，部分映射继续有效项；USB/协同保存反馈独立。316 checks、v2 1+4+20+6、USB-001..016 和 x64 Release 通过；dist 1.78 MiB，未执行真实硬件或网络操作。 |
| 2026-09-02 | DS-026 输入源空值与零值公共合同 | 已批准并应用；Windows Release 本轮未重复运行 | 本任务提交 | 公共 USB schema 的映射和动作最小值由 0 收紧为 1，保留 null、`missing_mapping` 和 USB-001..016 原向量；当前 macOS 环境仅运行共享合同检查，未修改 macOS 源码或 `PROTOCOL.md`。 |
| 2026-09-02 | DS-027 联动 DDC 统一控件 | 代码完成；实机交互待确认 | PR #71 | 设置与托盘共用公共投影，联动模式最多三个扁平控件；混合/未知、上限交集、RDP/不可信拓扑零目标和绝对值批量写入已加 fake 回归；本机 macOS 环境无 `pwsh`，PR CI run `33609057399` 已完成 x64 Release、334 checks、1.82 MiB 分发校验与 artifact 上传。 |
| 2026-09-03 | W-031 Windows 通知区域单色图标 | 代码完成；实机视觉待确认 | PR #74 | Shell 托盘项使用独立透明黑白线稿，按系统任务栏主题和 DPI 原位刷新；纯测试覆盖主题选择、fallback、16/20/24/32 像素边界与去重刷新。PR CI run `33715488641` 已完成 x64 Release、368 checks、1.84 MiB 分发校验和 artifact 上传。 |
| 2026-09-03 | W-033 Windows 托盘菜单图标与 DDC 行紧凑化 | 代码和 CI 完成；实机视觉待确认 | PR #78 | 退出改为同字重电源图标；DDC 标签列按实测内容动态收紧，滑杆间距 4 像素、96 DPI 最小宽度 260 像素；综合 Windows run `33823153891` 已通过 411 checks、x64 Release 与分发校验。 |
| 2026-09-04 | W-034 Windows 后台媒体键 DDC 路由 | 代码和 CI 完成；实机媒体键/DDC 待确认 | PR #78 | Raw Input 只观察标准音量媒体键及 HID Consumer Control 亮度 usage，不吞系统动作；动作时刷新可信物理拓扑，非联动同相对步进、联动确定公共值后同绝对写入，混合/未知/RDP 零写入，静音恢复仅驻留会话内存；run `33823153891` 全绿。 |

## PR #69 当前拓扑映射投影与共用输入源布局（2026-09-01）

- [x] USB 与协同映射列表只投影当前可信拓扑代次中 `Resolved` 且强身份唯一的物理显示器；不按显示名称或“显示器”文案过滤。
- [x] 历史离线、歧义和待确认显示器继续保留在本机目录；隐藏行保存时按显示器 ID 合并，既有 USB/协同映射不删除、不重绑定、不覆盖。
- [x] RDP、枚举失败、不完整或空结果不会清空最后可信映射投影，也不会触发目录保存。
- [x] USB 与协同页实际消费同一个三列动态映射网格：左侧固定“对端输入源”标签跨全部行并垂直居中，中间名称列为 Star，右侧输入框列固定宽度。
- [x] 纯模拟覆盖当前 2 台物理显示器 + 2 条历史离线目录：UI 投影仅 2 行，4 条目录及离线映射完整保留；同时覆盖 0、1、3 台以上布局行跨度。
- [x] Windows 全量自动测试通过 298 checks；`Windows/build-windows.ps1 -Architecture x64 -Configuration Release` 构建成功。
- [x] 使用最终 x64 Release 实际打开 USB 切换与协同页并截图：两页无崩溃、无空白，左侧标签跨当前 2 行垂直居中，列宽一致，历史无名条目未显示。验证仅切换页面，未执行网络、USB、DDC、输入源、唤醒或保存操作。
- [ ] 0、1、3 台以上实体显示器组合仍待更多实机确认；自动模型已覆盖。

## PR #69 重复强绑定双遍统计（2026-09-01）

- [x] `DisplayMappingProjection` 先筛选当前可信代次、`Resolved`、合法 displayId 和规范化后强绑定候选，再统计规范化 displayId/nativeMonitorId 频次，第二遍仅投影两类频次均为 1 的条目。
- [x] 重复强绑定涉及的全部条目均排除，不再保留第一项；重复 displayId 和仅大小写不同的强绑定同样全部排除。
- [x] 保留 2 台当前物理 + 2 条历史离线目录的既有测试，并验证目录和 USB/协同映射仍为 4 条；RDP/Incomplete 继续保留最后可信投影。
- [x] Windows 全量自动测试通过 302 checks；x64 Release 构建成功，绿色包 1.78 MiB。
- [ ] 最终两行脱敏截图待具备两台 `Resolved` 物理显示器的实机环境后补充。本次只读实机检查显示本机 4 条目录当前均为 Offline，最终 Release 因而正确显示 0 个映射行；未点击可能持久化目录的“重新检测显示器”，未执行或保存任何硬件操作。

### W-204 / W-034 审核修复：事件计时器与媒体键待写值

- [x] 接收入站协同请求时取消被替换出站事件的重试与兜底计时器，延迟唤醒完成不能错误触发本机切屏。
- [x] 媒体键成功写入只清除匹配值的待写投影，保留更新请求；发布缓存与清理投影共用媒体键锁，逐次规划合并最新缓存和本次物理拓扑。
- [x] 补充滑杆值更新后的相对步进、旧完成保留新请求、双向请求重叠与延迟唤醒回归用例。
- Windows x64 Release 构建及完整原生回归由本次 PR 的 Windows GitHub Actions 执行，验收以该检查结果为准；macOS 上的纯 C++ 模拟仅作补充证据。
- [ ] 实机连续媒体键/托盘滑杆交错、双向协同请求重叠仍需验证。

- W-034 连续操作补充：托盘滑杆与媒体键在同一锁边界更新投影并入队，覆盖未执行媒体请求也会替换待写值；旧成功/失败仅清理匹配值，过期代次失败不影响新会话。新增覆盖后的 85/90 步进回归。

### W-035 SFlip 对外命名统一

- [x] 产品元数据、关于页、托盘提示、诊断标题、启动器与 CI 附件统一为 SFlip。
- [x] 新入口为 SFlip.exe，登录启动选择新入口；配置目录、注册项名称、协议与内部 runtime 名称保持兼容。
- 本机为 macOS，完整 Windows 原生测试、x64 Release 和分发校验由本次 PR 的 Windows CI 验证。
- [ ] 旧版升级后的快捷方式、登录启动及实际界面待 Windows 实机验证。

### W-036 Windows Explorer 重建后托盘图标恢复

- [x] 隐藏托盘窗口处理系统注册的 `TaskbarCreated` 广播；Explorer 重建通知区域后强制按当前 DPI 与主题刷新图标，并恢复完整提示、回调及 `NOTIFYICON_VERSION_4`。
- [x] 恢复先用 `NIM_MODIFY` 幂等确认同一 GUID 通知项，失败时才执行 `NIM_ADD`；状态、气泡或主题刷新发现 `NIM_MODIFY` 失败时进入同一路径。
- [x] Shell 暂时不可用时立即首试并以 500 ms 间隔最多尝试 8 次；成功、上限耗尽或销毁时停止定时器。重试期间的重复广播刷新外观但不重置次数或延后计时。
- [x] 纯模拟调用生产恢复 helper，覆盖已有图标、丢失后新增、`SETVERSION` 失败后只修改已有项、持续失败上限及版本失败保留已有项；不访问真实任务栏或重启 Explorer。
- 本机缺少 64 位 MSBuild、Visual Studio C++ 桌面开发和 Windows App SDK C++ 组件，x64 Release 构建在 `Windows/build-windows.ps1` 第 32 行按预期停止；完整编译与自动测试以本次 PR 的 Windows CI 为准。
- [ ] Explorer 重启、主显示器 DPI 变化、主题切换期间恢复及退出时迟到定时消息仍需 Windows 实机验证；本任务未重启 Explorer/SFlip，未执行 USB、DDC、输入源、网络或系统设置操作。

2026-09-07 发布校验补充：Inno Setup 的 EXE 文件版本默认 0.0.0.0，与 AppVersion 独立；现让 VersionInfoVersion 跟随应用资源，并在打包脚本逐项检查四段版本，失败则拒绝交付。

2026-09-11：用户授权发布 Windows v2.3.2（build 23），包含已合并的 W-036 托盘恢复修复。版本资源与下载文档同步更新；正式构建、自动测试和发布包校验以发布 PR CI 与 Release 记录为准。macOS 保持 v2.3.0，本次不改协议或配置。


### W-037 对端离线或首次检测前可开启协同

- [x] 界面及 v5 保存校验只要求本机地址、端口、配对密码与输入映射完整，不以首次身份绑定或实时在线状态阻止开启；重启保留用户选择。
- [x] 开启意愿与运行时资格分开：协同目标必须身份已确认、协议 v2、非本机且在已开启配置中唯一；未绑定配置仅可参与既有首次探测，手动切换回退入口也执行同一资格检查。
- [x] 配置详情显示未开启、已开启待确认或已开启；保存成功后即时刷新，缺少本机字段时列出具体缺项，移除旧本机触发设备摘要。
- [x] 新增模拟回归覆盖未绑定开启的保存/加载/关闭、自动连接状态、已绑定离线开启、无硬件状态机输出、路由冲突、安全状态、非法配置及原子保存失败；确认流程已由 W-039 移除。
- 本机缺少 MSBuild；完整原生测试与 x64 Release 构建由本次 PR 的 Windows CI 验证，最终结果见 PR。
- [ ] Windows 实机开启、重启保持、对端上线后手动检测确认及长文本布局待验；未执行真实网络、USB、DDC 或系统设置操作。

### W-038 双端绑定状态不一致时的 Windows 探测回复

- [x] 已绑定发送端向尚未绑定的 Windows 发出指向本机的 v2 状态探测时，完成严格验证后仅回复状态；已有正常协同路由保持原行为。
- [x] W-038 原有身份确认边界已由 W-039 配对码直接连接取代；来源地址与端口、配对认证、时间窗、重放、歧义拒绝及零硬件动作继续保留。
- [x] 增加固定白名单探测诊断，区分收到请求、匹配失败与回复发送结果，不记录报文或原始身份信息。
- [ ] 模拟回归与 x64 Release 构建通过；实机双向检测仍需单独验收。

### W-039 配对码直接连接与自动路由缓存

- [x] 本机字段完整的配置可在没有对端路由缓存时开启；应用自动启动监听，并为已开启配置周期发送 targetEndpointID = null 的认证状态探测。
- [x] 所有 status_probe 按实际来源地址、端口和配对码唯一匹配；旧缓存 target 只作提示，原始报文 HMAC、时间窗和 nonce/重放仍严格验证，其他消息继续要求当前本机 target。
- [x] 有效探测或匹配待处理事件的响应自动更新 v5 路由缓存，不弹身份确认；缓存先原子保存再发布，相同缓存不重复保存，变化仅更新路由并取消引用旧路由的事件，保留其他配置的活动事件与计时器。
- [x] 已关闭配置可更新缓存并回复状态探测但保持关闭；歧义、错误来源、错误配对码、保存失败和安全状态均不连接、不回复且不执行硬件动作。
- [x] 设置页移除首次及变化身份确认流程，只显示未开启、正在自动连接或已开启状态。
- [ ] Windows 原生回归、x64 Release、安装器检查和双端状态连接实机验证待完成；实机不得执行切屏、USB 交接、DDC 或唤醒。

### W-040 USB 与协同设置独立保存

- [x] 协同保存只捕获、校验并合并协同配置，不因 USB 页面状态失败或跳转；USB 设置和联动选择保持原值。
- [x] USB 保存只捕获、校验并合并 USB 配置，不提交或校验无关的协同草稿；常规和显示器保存继续保持各自字段边界。
- [x] USB 联动允许引用仍存在但暂时关闭或正在补充的协同配置并跨重启保留意愿；运行时继续阻止网络唤醒，重新开启协同后恢复联动。
- [x] 删除被 USB 联动引用的协同配置时，同次原子保存关闭联动并清空引用，避免嵌套保存和非法持久状态。
- [x] 模拟回归覆盖双向页面隔离、disabled 引用保存加载、删除清理和零网络唤醒；本机 x64 Release 因缺少 64 位 MSBuild 无法执行，待 CI 验证。

### W-041 Windows 设置页文案精简

- [x] 移除协同页“先检查权限”和零硬件副作用的常驻说明，保留“检查网络权限”“检测连接”两个明确操作。
- [x] 移除诊断页重复解释预览来源、零副作用和复制一致性的常驻说明，保留刷新、复制和只读预览。
- [x] 网络权限检查、前置条件失败及权限结果改为简短行动文案，不显示 UDP、协议或内部实现术语。
- [x] 协同连接状态移到配置内容之后的页面底部；检测结果和权限提示继续使用既有底部反馈区，操作按钮及配置项位置不变。
- [x] 协同页只显示一处当前状态：连接成功统一显示对端配置名，网络检查和手动检测不再叠加成功提示；必要错误与保存反馈继续保留。
- [ ] x64 Release 构建、完整原生回归和设置页视觉验收待完成；本任务不改变任何权限、网络、诊断导出或硬件行为。

### W-042 连接与配置保存恢复一致性

- [x] 周期探测在既有 event 未过期时保留待处理响应，成功消费后下一周期继续发送；过期后才重新探测，迟到旧响应仍拒绝。
- [x] 保存失败后的同值重试真正执行持久化；只有完整保存成功后才解除当前运行时和设置页安全状态，失败继续阻断副作用。
- [x] 后台认证路由缓存轻量同步到已打开设置页的已保存副本及匹配草稿，其他页面保存不回滚新缓存；修改地址、端口或配对码会失效旧缓存，NFC 等价配对码保留。
- [x] 添加生产 helper 模拟回归覆盖 2.1 秒响应、超时重试、保存故障与同值恢复、General/USB 保存保留缓存及草稿隔离，不访问真实网络或硬件。
- [x] 提交 `6560391` 通过 [Windows CI run 34680995062](https://github.com/maizihk/SFlip/actions/runs/34680995062)：503 项原生检查、61 项安装器检查和 x64 Release 构建全部成功；本机仍缺少 64 位 MSBuild。
- [ ] 保存恢复和多配置状态仍需设置页实机验收。

### W-043 用户文档精简

- [x] 根 README 和 Windows README 改为安装、使用及常见问题说明，删除内部工程记录与重复解释，保留运行库依赖、完整绿色版目录及升级配置信息。
- [x] `docs/releases/v2.3.0.md`、`v2.3.1.md`、`v2.3.2.md` 的文案通过 GitHub REST 更新到现有 Release；版本及附件保持不变。
- [x] 本地 Markdown 链接和 `git diff --check` 验证通过；本次为纯文档修改，未新增平台构建，前置代码 `6560391` 双端 CI 已成功。
